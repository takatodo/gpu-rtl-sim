# AGENTS.md

Codex-facing project instructions have moved to:

```text
for_codex/AGENTS.md
```

Codex agents working in this repository should read that file before making changes. The root file is intentionally only a pointer so external users do not have to read agent-specific operating instructions.
