# GPU Toggle Coverage Minimal

Minimal extraction of the GPU-toggle coverage hybrid-runtime project.

The repository keeps the active implementation surface small while preserving a reproducible path from configuration to build, run, comparison, and status reporting. Generated outputs are not source of truth.

## Goal

Large goal:

`modern_llm_serving_rtl_hybrid_conditions`

Current priority:

`review_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_implementation_gate`

Current gate (authorizing artifact):

`config/scaling_gates/implement_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate.json`

The larger project question is: identify the conditions where a hybrid CPU/GPU RTL coverage runtime can run modern LLM-serving-like RTL workloads correctly and quickly.

External-facing result synthesis:

- `docs/results.md`
- `python3 src/tools/run_results_reproduction.py --dry-run`
- `python3 src/tools/run_results_reproduction.py --repeat-median 3 --dry-run`
- `python3 src/tools/run_results_reproduction.py --paged-kv-continuation-repeat-median 3 --dry-run`
- `python3 src/tools/run_results_reproduction.py --paged-kv-next-shapes-repeat-median 3 --dry-run`
- `python3 src/tools/run_results_reproduction.py --filelist-shape-breadth-repeat-median 3 --dry-run`
- `python3 src/tools/run_results_reproduction.py --filelist-broader-shape-repeat-median 3 --dry-run`
- `python3 src/tools/run_results_reproduction.py --filelist-broader-policy-repeat-median 3 --dry-run`
- `python3 src/tools/run_results_reproduction.py --filelist-shape-breadth-gpu-allocation-policy --dry-run`
- `python3 src/tools/run_results_reproduction.py --filelist-broader-shape-gpu-allocation-policy --dry-run`
- `python3 src/tools/run_results_reproduction.py --resident-batch-sweep 1,8,16,32 --resident-batch-sweep-repeat 3 --dry-run`
- `python3 src/tools/run_results_reproduction.py --resident-state-reuse 16x64 --resident-state-reuse-phases 4 --dry-run`
- `python3 src/tools/run_results_reproduction.py --persistent-resident-state-abi 16x64 --persistent-resident-state-abi-phases 4 --dry-run`
- `python3 src/tools/run_results_reproduction.py --persistent-resident-state-abi-shape-phase-sweep --dry-run`
- `python3 src/tools/run_results_reproduction.py --mobile-vit-imagenet-128 --dry-run`
- `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/nvdla_cmac_a2cacc.json --shape 1x1 --dry-run`
- `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/prim_count.json --shape 1x1 --dry-run`
- `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/prim_secded_inv_39_32_enc.json --shape 1x1 --dry-run`
- `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/tlul_fifo_sync.json --shape 1x1 --dry-run`
- `python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 64x1 --dry-run`
- `python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --shape 64x1 --dry-run`
- `python3 src/tools/run_hybrid_benchmark.py --minimal-bench-suite`
- `python3 src/tools/run_hybrid_benchmark.py --run-minimal-bench-suite`
- `python3 src/tools/run_hybrid_benchmark.py mobile_vit --limit 128 --dry-run`
- `python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 1x1 --summary-out reports/hybrid_benchmark_pulp_ita_mha_template_1x1.json`
- `python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --shape 1x1 --summary-out reports/hybrid_benchmark_paged_attention_kv_score_template_1x1.json`
- `python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 1x1 --mode resident-state-reuse --phases 2 --summary-out reports/hybrid_benchmark_pulp_ita_mha_resident_state_reuse_1x1.json`
- `python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 1x1 --mode persistent-resident-state-abi --phases 2 --summary-out reports/hybrid_benchmark_pulp_ita_mha_persistent_resident_state_abi_1x1.json`
- `python3 src/tools/run_hybrid_benchmark.py mobile_vit --limit 128 --summary-from-existing --summary-out reports/hybrid_benchmark_mobile_vit_template_limit128.json`

How to read the public benchmark pack:

- Start at `docs/results.md`.
- Use `docs/tool_surface.md` to distinguish routine CLI entrypoints from helper modules.
- Treat `config/selection.json` (compact pointer and core fields), `config/selection_extensions.json` (linked `completed_goal_evidence` and MobileViT accuracy block), and `config/selection_verification_commands.json` (linked scripted verification checklist), together with `docs/status.md`, `docs/roadmap.md`, and this README, as the current source of truth. Merge programmatically via `src/tools/selection_state.py` (`load_selection`) when tooling needs the historical maps and full verification command list.
- Treat `reports/` and `artifacts/` as generated evidence/output only.
- Review `reports/hybrid_benchmark_*.json` for the compact wrapper summary schema: target, shape or limit, execution mode, commands, expected reports, and collected evidence.
- Prefer `--dry-run` first when reproducing measurements.
- External pack boundary: a review bundle may include generated `reports/*.json` as evidence, but a clean checkout can regenerate them from the documented commands.

## Commit Guard

Enable the versioned pre-commit hook once per checkout:

```sh
git config core.hooksPath .githooks
```

The hook runs `python3 src/tools/check_staged_large_files.py`, rejects staged files larger than `5 MiB`, rejects commits with more than `100` staged non-submodule file changes including deletions and type changes, rejects guarded script deltas above `250` added lines per script, rejects guarded scripts above `300` total lines per script, rejects more than `3` new guarded scripts in one commit, and rejects growing `tests/contract/test_*.py` files above `1200` total lines or `250` added lines. Existing oversized contract tests may still shrink. Guarded scripts are Python tools under `src/tools/` and shell hooks under `.githooks/`. Override local thresholds with `GPU_TOGGLE_MAX_COMMIT_FILE_BYTES=<bytes>`, `GPU_TOGGLE_MAX_COMMIT_FILE_COUNT=<count>`, `GPU_TOGGLE_MAX_SCRIPT_ADDED_LINES=<lines>`, `GPU_TOGGLE_MAX_SCRIPT_TOTAL_LINES=<lines>`, `GPU_TOGGLE_MAX_NEW_SCRIPT_FILES=<count>`, `GPU_TOGGLE_MAX_CONTRACT_TEST_ADDED_LINES=<lines>`, or `GPU_TOGGLE_MAX_CONTRACT_TEST_TOTAL_LINES=<lines>` only when a reviewed change genuinely needs a different limit.

Commit cadence: keep commits small enough to review by one gate or one workflow boundary. Prefer committing source/doc/test changes separately from generated `reports/` refreshes; keep `artifacts/` as local generated output and do not commit bulky generated outputs as source of truth.
- Public pack manifest: `docs/results.md` lists the minimum source-of-truth files, `records/scaling_gates` gate/audit records, tools, templates, contract tests, and optional generated evidence snapshots to include for review.
- Verilator native option parser overlay patch descriptor/apply-check implementation reviewed: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_descriptor_apply_check_implementation_gate.json` accepts only the descriptor JSON and `git apply --check` evidence for the repo-owned overlay patch. The next task is defining a build-only validation gate; this still adds no native parser support, upstream landing, Verilator build success, execution, timing, arbitrary RTL/filelist support, or automatic GPU allocation claim.
- Verilator native option parser overlay patch build-only validation boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_build_only_validation_gate.json` selects an external clean-checkout apply plus `make ... verilator_bin` command sequence and advances the current task to review. This definition still does not run the build or claim native parser support, upstream landing, parser behavior, execution, timing, arbitrary RTL/filelist support, or automatic GPU allocation.
- Verilator native option parser overlay patch build-only validation boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_build_only_validation_gate.json` accepts the narrow `verilator_bin` build boundary and advances the current task to the build-only run gate. The run must record whether `VERILATOR_INSTALL` was supplied or mechanically defaulted; this still adds no parser behavior, upstream regression, sidecar execution, timing, arbitrary RTL/filelist support, or automatic allocation claim.
- Verilator native option parser overlay patch build-only validation run failed: `config/scaling_gates/run_verilator_native_option_parser_overlay_patch_build_only_validation_gate.json` records `verilator_bin` reached and `make` exit code `2` due to `V3Options.h` patch placement after the header guard. The current task is defining the compile-fix boundary.
- Verilator native option parser overlay patch compile-fix boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_compile_fix_gate.json` keeps the existing patch path, rejects zero-context hunks, and pins contextual anchors for `V3Options.h` plus `V3Options.cpp`. The current task is reviewing that boundary before changing the patch or rerunning `verilator_bin`.
- Verilator native option parser overlay patch compile-fix boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_compile_fix_gate.json` accepts the in-place contextual patch repair and advances the current task to `implement_verilator_native_option_parser_overlay_patch_compile_fix_gate`.
- Verilator native option parser overlay patch compile-fix implemented: `config/scaling_gates/implement_verilator_native_option_parser_overlay_patch_compile_fix_gate.json` records the contextual patch repair, descriptor validation, clean-checkout `git apply --check`, actual patch apply, and post-apply location sanity. `make verilator_bin` is intentionally deferred to `run_verilator_native_option_parser_overlay_patch_compile_fix_build_only_validation_gate`; this still adds no build success, parser behavior, execution, timing, arbitrary RTL/filelist, or automatic allocation claim.
- Verilator native option parser overlay patch compile-fix build-only validation passed: `config/scaling_gates/run_verilator_native_option_parser_overlay_patch_compile_fix_build_only_validation_gate.json` records descriptor validation, clean-checkout `git apply --check`, actual patch apply, `autoconf`, `configure`, and `make -j 28 verilator_bin` exiting `0` with a checkout-local generated `VERILATOR_INSTALL` default. The current task is reviewing that build-only evidence before claiming parser behavior, sidecar execution, timing, arbitrary RTL/filelist support, or automatic allocation.
- Verilator native option parser overlay patch compile-fix build-only validation reviewed: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_compile_fix_build_only_validation_gate.json` accepts only compile/link compatibility for the repaired patch and advances the current task to defining parser-behavior checks. Native parser support, sidecar execution, timing, arbitrary RTL/filelist support, and automatic allocation remain non-claims.
- Verilator native option parser overlay patch parser-behavior boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_parser_behavior_gate.json` selects a built-binary lint-only smoke boundary for expanded `--sim-accel sidecar-gpu --sim-accel-states <N> --sim-accel-steps <S>` cases and the smallest invalid parser cases. It explicitly defers strict `std::atoi` suffix hardening and still adds no parser execution, sidecar execution, timing, arbitrary RTL/filelist support, or automatic allocation claim.
- Verilator native option parser overlay patch parser-behavior boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_parser_behavior_gate.json` accepts the boundary as narrow enough and advances the current task to the parser-only built-binary smoke run. This still adds no parser behavior result, native parser support, sidecar execution, timing, arbitrary RTL/filelist support, or automatic allocation claim.
- Verilator native option parser overlay patch parser-behavior smoke passed: `config/scaling_gates/run_verilator_native_option_parser_overlay_patch_parser_behavior_gate.json` records the two positive expanded spellings exiting `0` and the six required rejection cases exiting `1` with expected diagnostics. This is a scoped parser-only smoke result; it still adds no sidecar execution, strict integer parsing, timing, arbitrary RTL/filelist support, or automatic allocation claim.
- Verilator native option parser overlay patch parser-behavior smoke reviewed: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_parser_behavior_run_gate.json` accepts only the scoped parser-only smoke result and advances the current task to defining parser integer hardening for negative counts and `std::atoi` suffix inputs. This still adds no strict integer parsing result, sidecar handoff, execution, timing, arbitrary RTL/filelist support, or automatic allocation claim.
- Verilator native option parser overlay patch parser-integer hardening boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate.json` selects separate-token negative count cases and non-numeric suffix cases as the next strict-integer boundary, with tokenizer ambiguity explicitly recorded. This definition adds no patch change, hardening result, sidecar handoff, execution, timing, arbitrary RTL/filelist support, or automatic allocation claim.
- Verilator native option parser overlay patch parser-integer hardening boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate.json` accepts the boundary and advances the current task to in-place overlay patch implementation. Suffix cases require replacing `std::atoi`; this still adds no hardening result, sidecar handoff, execution, timing, arbitrary RTL/filelist support, or automatic allocation claim.
- Verilator native option parser overlay patch parser-integer hardening smoke passed: `config/scaling_gates/run_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate.json` rebuilds the updated overlay patch from a clean Verilator `v5.048` checkout and records the two positive expanded parser cases plus four strict integer hardening rejection cases passing. This permits only the scoped parser-only `--sim-accel-states` / `--sim-accel-steps` hardening claim; sidecar handoff, execution, timing, arbitrary RTL/filelist support, and automatic allocation remain non-claims.
- Verilator native option parser overlay patch parser-integer hardening smoke reviewed: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_parser_integer_hardening_run_gate.json` accepts only the scoped strict count-token result and advances the current task to defining the parser-to-sidecar handoff boundary. This still adds no sidecar handoff validation, RTL simulation, timing, arbitrary RTL/filelist support, automatic allocation, upstream regression success, or upstream landing claim.
- Verilator native option parser-to-sidecar handoff boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_handoff_boundary_gate.json` pins a non-executing native-parser adapter payload before the existing sidecar stage plan. Parser-owned fields are limited to `sidecar-gpu`, positive state/step counts, normalized shape, ordinary Verilator args, and preserved build inputs; source closure, coverage target, state/report paths, host-probe metadata, and compare details remain sidecar-owned. The current task is reviewing that boundary, not claiming runtime handoff, RTL simulation, timing, arbitrary RTL/filelist support, automatic allocation, upstream regression success, or upstream landing.
- Verilator native option parser-to-sidecar handoff boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_handoff_boundary_gate.json` accepts only the adapter boundary and advances the current task to implementing a non-executing handoff fixture. `coverage_output_equivalence` is only a later sidecar correctness-policy reference here, not parser-populated compare evidence. The fixture must not populate resolved state files, generated reports, compare labels, coverage targets, manifest refs, host-probe metadata, runtime handoff, timing, arbitrary source closure, or automatic allocation.
- Verilator native option parser-to-sidecar handoff fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_handoff_fixture_gate.json` adds `src/tools/verilator_native_option_parser_sidecar_handoff.py`, an importable non-executing adapter helper. It preserves parser-owned Verilator inputs, emits `correctness_policy_ref` only as a later sidecar policy reference, rejects unexpected correctness-policy refs, and rejects parser values that already contain resolved sidecar outputs.
- Verilator native option parser-to-sidecar handoff fixture reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_handoff_fixture_implementation_gate.json` accepts only the non-executing fixture implementation and advances the current task to defining sidecar plan-resolution boundaries. This still does not claim native parser support, sidecar runtime handoff, RTL simulation, timing, arbitrary source closure, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_boundary_gate.json` requires explicit sidecar context such as target, mode, template or registry entry, and source gate/manifest reference before an adapter payload may reach `sidecar_stage_plan`. Parser-owned inputs remain schedule values and preserved Verilator build inputs; source closure, filelist expansion, coverage-output target selection, host-probe metadata, state/report paths, compare labels, final compare policy, execution, timing, and automatic allocation stay sidecar-owned. This definition does not claim native parser support, runtime handoff, RTL simulation, timing, arbitrary RTL/filelist support, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_boundary_gate.json` accepts only the explicit-context, non-executing planning definition and advances the current task to implementing a non-executing plan-resolution fixture. The fixture must reject payload-only resolution and still adds no native parser support, sidecar runtime handoff, RTL simulation, timing, arbitrary RTL/filelist support, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_plan_resolution_fixture_gate.json` adds `src/tools/verilator_native_option_parser_sidecar_plan_resolution.py`, an importable non-executing helper that requires reviewed adapter payload plus explicit target/mode/template or registry-entry context before calling `sidecar_stage_plan`. Parser files and filelists remain preserved inputs, not source closure or dependency expansion; this still adds no native parser execution, sidecar runtime handoff, RTL simulation, timing, arbitrary RTL/filelist support, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution fixture reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_fixture_implementation_gate.json` accepts only the explicit-context, non-executing template-plan fixture and selects status/readiness hardening next. The weak point is that an outer `planned` status can obscure not-ready or unsupported `sidecar_stage_plan` results for non-template modes; this still adds no handoff contract, command synthesis, execution, timing, arbitrary RTL/filelist support, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution status hardening defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_gate.json` requires the future helper to expose outer status as direct Verilator option readiness: `ready_for_verilator_option_shim`, `not_ready_for_verilator_option_shim`, or `unsupported_for_stage_plan`, while preserving nested `stage_plan_status`. This is definition-only and still adds no handoff contract, command synthesis, execution, timing, runtime/ABI, arbitrary RTL/filelist support, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution status hardening reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_gate.json` accepts the readiness-classified outer status contract and selects in-place helper hardening next. The implementation must add `plan_resolution_readiness`, preserve nested `stage_plan_status`, and keep handoff contract, command synthesis, execution, timing, runtime/ABI, arbitrary RTL/filelist support, and automatic allocation out of scope.
- Verilator native option parser-to-sidecar plan-resolution status hardening implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_gate.json` records in-place helper hardening. The helper now reports outer readiness status, preserves nested `stage_plan_status` through `plan_resolution_readiness`, and keeps sidecar handoff contract, command synthesis, execution, timing, runtime/ABI, arbitrary RTL/filelist support, and automatic allocation absent.
- Verilator native option parser-to-sidecar plan-resolution status hardening implementation reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_implementation_gate.json` accepts only the readiness-classified non-executing helper and advances the current task to defining the handoff-contract boundary after plan resolution. This still adds no executed handoff contract, command synthesis, RTL simulation, timing, runtime/ABI, arbitrary RTL/filelist support, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_boundary_gate.json` defines that only ready plan-resolution outputs may feed future `sidecar_handoff_contract` metadata. It separates metadata construction from runtime handoff execution, command synthesis, operator-plan production, RTL simulation, timing, runtime/ABI, arbitrary filelist expansion, and automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_boundary_gate.json` accepts only the ready-only metadata boundary and advances the current task to `implement_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_fixture_gate`. The implementation must fail closed before calling `sidecar_handoff_contract` for not-ready or unsupported plan-resolution outputs and still adds no command synthesis, operator plan, execution, timing, runtime/ABI, arbitrary filelist support, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_fixture_gate.json` adds a thin wrapper in `src/tools/verilator_native_option_parser_sidecar_plan_resolution.py` plus the importable support module `src/tools/verilator_native_option_parser_sidecar_handoff_contract.py`. Ready outputs produce metadata only; not-ready and unsupported outputs return structured closed results before calling `sidecar_handoff_contract`. That implementation advanced to review, with command synthesis, operator plan, execution, timing, runtime/ABI, arbitrary filelist support, and automatic allocation still out of scope.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract fixture implementation reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_fixture_implementation_gate.json` accepts only the metadata-only, ready-only helper after adding the explicit `efficiency_estimate_invoked` guard. It advances the current task to defining the handoff-contract-to-operator-plan boundary; this still adds no command execution, operator-plan execution, RTL simulation, compare execution, timing, runtime/ABI, arbitrary filelist support, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_boundary_gate.json` defines only non-executing command argv, estimate-command, efficiency-estimate, and operator-plan metadata from accepted ready-only handoff-contract metadata. It advances the current task to reviewing that boundary; this still adds no command execution, operator-plan execution, RTL simulation, compare execution, timing evidence, runtime/ABI, arbitrary filelist support, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_boundary_gate.json` accepts the metadata-only boundary and advances the current task to implementing an importable non-executing operator-plan fixture. `efficiency_estimate` is allowed only as planning metadata, not timing or speedup evidence; command execution, operator-plan execution, RTL simulation, compare execution, runtime/ABI, arbitrary filelist support, and automatic allocation remain out of scope.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture implementation reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_implementation_gate.json` accepts the metadata-only helper only for accepted ready handoff-contract fixture metadata. The review records two hardening gaps before any execution boundary: bool values are not yet rejected by positive-int checks, and `efficiency_estimate` may include existing-report-derived values that are not new timing evidence from this gate. It advanced the task to defining that hardening gate; this still adds no public CLI, command execution, operator-plan execution, RTL simulation, compare execution, measured timing, runtime/ABI change, arbitrary filelist expansion, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture hardening boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_gate.json` selects bool rejection for positive-int fields, scoped malformed Mapping fail-closed behavior, and `efficiency_estimate` non-timing wording before any execution boundary. That definition advanced the task to review; this still adds no implementation, public CLI, command execution, operator-plan execution, RTL simulation, compare execution, measured timing, runtime/ABI change, arbitrary filelist expansion, or automatic allocation.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture hardening boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_gate.json` accepts the minimum bool/int and scoped Mapping boundary, but strengthens the next implementation requirement so `stage_plan.phases` and `stage_plan.limit` bools must reject before any command, estimate, efficiency, or operator-plan authority. That review advanced the task to implementation without execution, timing, runtime/ABI, arbitrary filelist, or automatic allocation claims.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture hardening implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_gate.json` hardens the existing helper in place. It rejects bool values in handoff-contract counts, stage-plan phases/limit, hybrid-sidecar-run details, and parser-schedule counts before command, estimate, efficiency, or operator-plan authorities.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture hardening implementation reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_implementation_gate.json` accepts that strict metadata-only hardening and advances the current task to defining the first execution boundary. The next gate is definition-only; this still adds no public CLI, command execution, operator-plan execution, RTL simulation, compare execution, measured timing, runtime/ABI change, arbitrary filelist expansion, automatic allocation, or complete adversarial Mapping validation claim.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan execution boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_execution_boundary_gate.json` defines that a later run gate must validate accepted operator-plan metadata, recover or regenerate a reviewed sidecar stage plan, then execute only reviewed sidecar build/run/compare stages. This definition itself still adds no public CLI, command execution, operator-plan execution, RTL simulation, compare execution, measured timing, runtime/ABI change, arbitrary filelist expansion, automatic allocation, or native Verilator support claim.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan execution boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_execution_boundary_gate.json` accepts the definition and advances the current task to `run_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_sidecar_execution_gate`. The run may execute only reviewed sidecar build/run/compare stages derived from accepted ready metadata and a reviewed or regenerated stage plan; this review itself still adds no command execution, RTL simulation, compare execution, measured timing, arbitrary filelist support, automatic allocation, or native Verilator support claim.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan sidecar execution run passed: `config/scaling_gates/run_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_sidecar_execution_gate.json` records `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/pulp_ita_mha.json --shape 64x1` completing the reviewed seven-stage sidecar build/run/compare flow. The compare report passes `coverage_output_equivalence` with mismatch count `0` across `64` state pairs, but this is still not native Verilator option support, timing or speedup evidence, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan sidecar execution run reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_sidecar_execution_run_gate.json` accepts only the scoped `pulp_ita_mha 64x1` sidecar build/run/compare result and advances the current task to defining a direct native-Verilator command-path boundary. This review still adds no native Verilator option support, timing or speedup evidence, arbitrary filelist support, automatic allocation, runtime/ABI change, direct command execution, or raw full-state equality.
- Verilator native option parser direct command-path boundary defined: `config/scaling_gates/define_verilator_native_option_parser_direct_command_path_boundary_gate.json` defines the minimum expanded Verilator-facing spelling and the parser-owned versus sidecar-owned field boundary before any direct native command execution. This still adds no native Verilator option support, direct command execution, timing or speedup evidence, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Verilator native option parser direct command-path boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_direct_command_path_boundary_gate.json` accepts the definition only as a non-executing command-path boundary and advances the current task to defining a direct-command fixture contract. The review deliberately does not claim native Verilator option support, direct command execution, timing, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Verilator native option parser direct command-path fixture contract defined: `config/scaling_gates/define_verilator_native_option_parser_direct_command_path_fixture_gate.json` pins the future importable helper contract, accepted `64x1` expanded spelling, explicit sidecar context, sidecar-plan-boundary reference, and fail-closed cases before implementation. This still adds no helper implementation, native Verilator option support, direct command execution, timing, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Verilator native option parser direct command-path fixture contract reviewed: `config/scaling_gates/review_verilator_native_option_parser_direct_command_path_fixture_gate.json` accepts the non-executing fixture contract and advances the current task to implementing the importable helper. The implementation must preserve ordinary Verilator inputs, require explicit sidecar context, return only reference-style sidecar-plan metadata, and still not claim native Verilator option support, direct command execution, timing, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Verilator native option parser direct command-path fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_direct_command_path_fixture_gate.json` adds `src/tools/verilator_native_option_parser_direct_command_path_fixture.py` as an importable non-executing helper. It accepts the reviewed expanded `sidecar-gpu 64x1` case, requires explicit sidecar context, returns reference-only sidecar-plan-boundary metadata, and still adds no native Verilator option support, direct command execution, timing, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Verilator native option parser direct command-path fixture implementation reviewed: `config/scaling_gates/review_verilator_native_option_parser_direct_command_path_fixture_implementation_gate.json` accepts only the non-executing reference-boundary helper and records a required payload-validation hardening follow-up before materialization or execution. This still adds no native Verilator option support, sidecar stage materialization, direct command execution, timing, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Verilator native option parser direct command-path fixture payload validation hardening defined: `config/scaling_gates/define_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate.json` requires complete parser-stub payload identity/build-input fields before reference-only boundary metadata and advances to review. This is definition-only and still adds no helper change, native option support, sidecar stage materialization, direct command execution, timing, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Verilator native option parser direct command-path fixture payload validation hardening reviewed: `config/scaling_gates/review_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate.json` accepts exact parser-stub payload keyset validation as the next implementation boundary. The weak point is that the rule is intentionally rigid and the current helper is already at the line guard, so implementation must split or shrink cleanly. This still adds no helper change, native option support, sidecar stage materialization, direct command execution, timing, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Verilator native option parser direct command-path fixture payload validation hardening implemented: `config/scaling_gates/implement_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate.json` adds a split parser-payload validation helper and wires the direct fixture to require exact parser-stub handoff payloads before returning boundary metadata. This still adds no public CLI, native option support, sidecar stage materialization, direct command execution, timing, arbitrary filelist support, automatic allocation, runtime/ABI change, or raw full-state equality.
- Public reproduction smoke: `docs/results.md` lists the first dry-run commands to run before attempting full measurements.
- Public release checklist: `docs/results.md` lists the final source-of-truth, path hygiene, smoke, evidence, non-claim, and contract-test checks before handoff.
- Externalization readiness audit: `config/scaling_gates/public_benchmark_pack_externalization_readiness_audit.json` records the minimum review surfaces, smoke commands, release checks, and evidence policy for externalization.
- Public archive dry-run: `python3 src/tools/run_results_reproduction.py --public-pack-archive --dry-run` prints the include/exclude plan without creating an archive.
- Filelist shape-breadth GPU allocation policy broader shape sweep timing public refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_gate.json` pins the timing definition/result/review gates plus eight timing reports and four compare reports as generated evidence only, then selects `run_public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_gate`. It adds no new measurement, execution, runtime/ABI, repeat-median, broad speedup, GEM, native-Verilator-option, arbitrary-RTL, automatic-allocation, production-serving, or raw full-state-equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep timing public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_result_gate.json` records the public archive dry-run exiting `0`, and `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_review_gate.json` accepts the packaging-only refresh. The next task is defining public benchmark-pack externalization completion for this scoped timing refresh.
- Filelist shape-breadth GPU allocation policy broader shape sweep timing public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_gate.json` closes the publication boundary for the scoped single-run `64x1` / `1x64` timing refresh. The next task is selecting the next measured workstream; this completion adds no new measurement, execution, repeat-median, broad speedup, GEM, native Verilator, arbitrary RTL/dependency inference, runtime/ABI, production-serving, automatic allocation, or raw full-state equality claim.
- Next measured workstream selected after broader shape-sweep timing public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_public_pack_refresh_gate.json` selects `define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_timing_gate`. The reason is that the accepted `64x1` / `1x64` timing evidence is still single-run; repeat-median timing for the same four target/shape pairs should precede allocation-policy broadening, native Verilator option work, dependency inference, runtime/ABI work, GEM comparison, or production-serving claims.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_timing_gate.json` fixes the same two reviewed filelist-derived targets at `64x1` and `1x64`, repeat count `3`, and `coverage_output_equivalence`. It records that the existing `--filelist-shape-breadth-repeat-median` workflow is scoped to `32x1` / `1x32`, so the next task is adding a distinct public workflow for the broader-shape set. This definition adds no measurement, execution, repeat-median evidence, broad speedup, GEM, native Verilator, arbitrary RTL/dependency inference, runtime/ABI, production-serving, automatic allocation, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median workflow added: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_workflow_gate.json` adds `python3 src/tools/run_results_reproduction.py --filelist-broader-shape-repeat-median 3 --dry-run`, expanding the two reviewed filelist-derived targets at `64x1` and `1x64` into three samples each without writing generated evidence in dry-run. The workflow gate adds no timing evidence by itself; the following measurement gate records the scoped repeat-median result.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median measured: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_measurement_gate.json` records `python3 src/tools/run_results_reproduction.py --filelist-broader-shape-repeat-median 3` exiting `0`. All 12 samples pass `coverage_output_equivalence` with mismatch count `0`; median CPU-to-hybrid wall ratios are about `129.03x` and `121.47x` for `64x1`, versus about `2.20x` and `1.74x` for `1x64`. This is scoped repeat-count-3 evidence only, not broad speedup, native Verilator option support, arbitrary RTL dependency inference, automatic GPU allocation, or production serving evidence.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median reviewed: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_review_gate.json` accepts only the scoped repeat-count-3 evidence for the same two filelist-derived targets at `64x1` and `1x64`, records the weak points, and selects public results packaging refresh definition next. It does not broaden the claim to native Verilator option support, arbitrary RTL dependency inference, automatic GPU allocation, runtime/ABI improvement, GEM comparison, or production serving.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median public refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_gate.json` pins the boundary, workflow, measurement, and review gates plus `reports/filelist_broader_shape_repeat_median_summary.json` and four median reports as generated evidence only. The next task is the public-pack archive dry-run; this adds no measurement, runtime/ABI change, arbitrary dependency inference, automatic allocation, GEM comparison, native Verilator option support, or production serving claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_result_gate.json` records the public-pack archive dry-run exiting `0`, and `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_review_gate.json` accepts the packaging-only refresh. The next task is defining public benchmark-pack externalization completion for this scoped repeat-count-3 evidence.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_gate.json` closes the publication boundary for the scoped repeat-count-3 `64x1` / `1x64` refresh. The next task is selecting the next measured workstream; this completion adds no new measurement, runtime/ABI change, arbitrary dependency inference, automatic allocation, GEM comparison, native Verilator option support, or production serving claim.
- Next measured workstream selected after broader shape-sweep repeat-median public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_public_pack_refresh_gate.json` selects `define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_gate`. The reason is that policy broadening was previously deferred until the `64x1` / `1x64` broader-shape timing evidence had repeat-median backing; this selection is not automatic optimal allocation, native Verilator option support, arbitrary RTL/dependency inference, GEM comparison, runtime/ABI change, or production serving evidence.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_gate.json` defines a sidecar policy boundary that recommends `64x1` with high confidence for the two reviewed filelist-derived targets and keeps `32x1` as a reviewed fallback. The next task is adding a distinct workflow for this broader policy surface; this definition adds no execution, measurement, native Verilator option, arbitrary RTL/dependency inference, runtime/ABI, GEM comparison, automatic optimal allocation, or production serving claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy workflow added: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_workflow_gate.json` adds `python3 src/tools/run_results_reproduction.py --filelist-broader-shape-gpu-allocation-policy --dry-run` as a distinct JSON-only dry-run surface for the scoped `64x1` recommendation and reviewed `32x1` fallback. The dry-run has been reviewed and the next task is the public-pack archive dry-run; this workflow adds no execution, measurement, native Verilator option, arbitrary RTL/dependency inference, runtime/ABI, GEM comparison, automatic optimal allocation, or production serving claim.
- Next measured workstream selected after broader shape-sweep policy public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_public_pack_refresh_gate.json` selects `define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate`. This is selection-only for scoped policy execution definition; it adds no execution, measurement, timing, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, runtime/ABI, GEM, production-serving, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy non-dry-run execution boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate.json` selects the two policy-recommended `64x1` build/run/compare commands for the reviewed filelist-derived targets. The `32x1` fallback stays recorded but not selected for this execution gate; this definition adds no execution, measurement, timing, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, runtime/ABI, GEM, production-serving, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy non-dry-run execution accepted: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_result_gate.json` records both policy-recommended `64x1` build/run/compare commands exiting `0`, writing the expected compare reports, and passing `coverage_output_equivalence` with mismatch count `0`; `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_review_gate.json` accepts only that scoped result. The next task is defining the public results packaging refresh for this accepted execution; this adds no timing, speedup, repeat-median, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, runtime/ABI, GEM, production-serving, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy non-dry-run public refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate.json` pins the accepted execution definition, result, review, two `64x1` compare reports, and broader policy input evidence as public-pack generated evidence. The next task is the public-pack archive dry-run; this adds no new execution, measurement, timing, speedup, repeat-median, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, runtime/ABI, GEM, production-serving, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy non-dry-run public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_result_gate.json` records the public-pack archive dry-run exiting `0`, `include_count=521`, `exclude_count=2`, and no archive creation. `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_review_gate.json` accepts the packaging-only refresh and selects the public benchmark-pack externalization completion definition next.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy non-dry-run public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate.json` closes the publication boundary for the scoped two-command `64x1` policy-selected execution result and refresh. The next task is selecting the next measured workstream; this completion adds no new measurement, execution, timing/speedup, repeat-median, GEM comparison, native Verilator option support, arbitrary RTL/dependency inference, automatic optimal allocation, runtime/ABI change, production-serving throughput, or raw full-state equality claim.
- Next measured workstream selected after broader policy non-dry-run public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_public_pack_refresh_gate.json` selects policy-selected `64x1` repeat-median timing definition next. Native Verilator option work, broader automatic allocation, dependency inference, resident optimization, GEM comparison, runtime/ABI change, production-serving, and raw full-state equality remain deferred.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median timing boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_timing_gate.json` fixes repeat count `3` for exactly the two policy-selected `64x1` filelist commands and records that a distinct workflow is needed. This definition adds no measurement, execution, timing/speedup, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, GEM, production-serving, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median workflow added: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_workflow_gate.json` adds `python3 src/tools/run_results_reproduction.py --filelist-broader-policy-repeat-median 3 --dry-run` for exactly the two policy-selected `64x1` filelist workloads. The next task is the dry-run gate; this adds no measurement, timing/speedup, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, GEM, production-serving, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median dry-run accepted: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_dry_run_result_gate.json` records the public dry-run exiting `0` with six compare plans, two `64x1` targets, no `1x64` plans, no file writes, and repo-relative output. `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_dry_run_review_gate.json` accepts only that plan and selects the non-dry-run timing gate next; this still adds no timing/speedup, repeat-median result, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, GEM, production-serving, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median measured and reviewed: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_measurement_gate.json` records `python3 src/tools/run_results_reproduction.py --filelist-broader-policy-repeat-median 3` for the same two `64x1` targets, six samples, and `coverage_output_equivalence` mismatch count `0`. Median CPU-to-hybrid-wall ratios are `145.7001090512541` for `filelist_paged_attention_kv_score` and `139.77687626774846` for `filelist_known_template_pulp_ita_mha`; `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_review_gate.json` accepts only this scoped result and selects the public results packaging refresh definition next. Per-sample stdout reports may contain local paths and are not public-pack evidence; aggregate and median JSON reports remain generated evidence only.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_gate.json` defines the packaging-only refresh for the accepted aggregate and median JSON evidence. `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_result_gate.json` records the public archive dry-run exiting `0`, `include_count=535`, `exclude_count=2`, no archive creation, and no per-sample stdout or local absolute paths in the manifest. `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_review_gate.json` accepts the refresh and selects public benchmark-pack externalization completion next.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_gate.json` closes the publication boundary for the scoped policy-selected `64x1` repeat-median result and refresh. The next task is selecting the next measured workstream; this completion adds no new measurement, execution, automatic optimal allocation, native Verilator option, arbitrary RTL/dependency inference, GEM, runtime/ABI, production-serving, or raw full-state equality claim.
- Next measured workstream selected after policy-selected repeat-median public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_public_pack_refresh_gate.json` selects `define_verilator_native_option_prototype_boundary_gate`. The reason is that the sidecar workflow now has scoped filelist-derived correctness, timing, policy-selected `64x1` repeat-median evidence, and public-pack closure; the next gap is defining the smallest native-Verilator-option prototype boundary without claiming arbitrary RTL support, dependency inference, automatic optimal allocation, runtime/ABI change, GEM comparison, or production-serving behavior.
- Verilator native option prototype boundary defined: `config/scaling_gates/define_verilator_native_option_prototype_boundary_gate.json` pins the future primary spelling as `verilator --cc --timing ... --sim-accel sidecar-gpu --sim-accel-states 64 --sim-accel-steps 1`, keeps `--sim-accel-shape <NxS>` as wrapper/shim compatibility, and selects the dry-run boundary next. Current shim previews use registered targets such as `paged_attention_kv_score` and `pulp_ita_mha`; filelist-derived `64x1` evidence remains carried as scoped input evidence until filelist targets are registered or a filelist-to-shim planner exists. This definition adds no native Verilator implementation, execution, measurement, runtime/ABI change, arbitrary RTL/dependency inference, automatic optimal allocation, GEM comparison, production-serving, or raw full-state equality claim.
- Verilator native option prototype boundary dry-run accepted: `config/scaling_gates/verilator_native_option_prototype_boundary_dry_run_result_gate.json` records four preview/dry-run commands exiting `0`: two registered shim previews and two filelist-template dry-runs at `64x1`. `config/scaling_gates/verilator_native_option_prototype_boundary_dry_run_review_gate.json` accepts only that plan-only result and advances to deciding whether `filelist_*` should become direct shim registry targets or stay behind a filelist-to-shim planner. This adds no native Verilator parser, direct filelist target support, execution, measurement, automatic allocation, dependency inference, GEM, production-serving, runtime/ABI, or raw full-state equality claim.
- Verilator native option prototype filelist registry support accepted: `config/scaling_gates/define_verilator_native_option_prototype_filelist_target_resolution_gate.json` selects static registry entries for exactly `filelist_paged_attention_kv_score` and `filelist_known_template_pulp_ita_mha`. `config/scaling_gates/verilator_native_option_prototype_filelist_registry_result_gate.json` records shim preview and operator-plan commands exiting `0`; `config/scaling_gates/verilator_native_option_prototype_filelist_registry_review_gate.json` accepts only tracked-template registry support and selects public results packaging refresh next. This adds no new planner, native Verilator parser, arbitrary filelist/RTL support, dependency inference, automatic allocation, execution measurement, runtime/ABI, GEM, production-serving, or raw full-state equality claim.
- Verilator native option prototype filelist registry public refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_verilator_native_option_prototype_filelist_registry_gate.json` defines the packaging-only refresh for the accepted tracked-template registry support and selects the public-pack archive dry-run next. This adds no new planner, native Verilator parser, arbitrary filelist/RTL support, dependency inference, automatic allocation, execution measurement, runtime/ABI, GEM, production-serving, or raw full-state equality claim.
- Verilator native option prototype filelist registry public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_verilator_native_option_prototype_filelist_registry_result_gate.json` records the public archive dry-run exiting `0`, `include_count=546`, `exclude_count=2`, no archive creation, and generated-output exclusions preserved. `config/scaling_gates/public_results_packaging_refresh_after_verilator_native_option_prototype_filelist_registry_review_gate.json` accepts only that packaging result and selects public benchmark-pack externalization completion next.
- Verilator native option prototype filelist registry public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_verilator_native_option_prototype_filelist_registry_gate.json` closes the publication boundary for the two static tracked `filelist_*` registry targets and selects the next measurement/workstream decision. This adds no new execution, measurement, native Verilator parser, arbitrary filelist/RTL support, dependency inference, automatic allocation, runtime/ABI, GEM, production-serving, or raw full-state equality claim.
- Next workstream selected after Verilator native option prototype filelist registry public-pack refresh: `config/scaling_gates/next_measurement_selection_after_verilator_native_option_prototype_filelist_registry_public_pack_refresh_gate.json` selects `define_commit_split_and_public_pack_cleanup_gate`. The reason is operational rather than algorithmic: the tracked registry/public-pack chain is closed, but the staged set is above the documented `100`-file commit guard, so the next gate must pin commit split boundaries before adding parser, planner, dependency inference, allocation, resident optimization, or GEM work.
- Commit split and public-pack cleanup boundary defined: `config/scaling_gates/define_commit_split_and_public_pack_cleanup_gate.json` pins eight review-sized commit groups with estimated file counts `15`, `26`, `71`, `30`, `24`, `59`, `28`, and `25`, all below the documented `100`-file hook limit. It also records the remaining per-file line guard risks for `src/tools/results_reproduction_gpu_allocation_policy.py`, `tests/contract/test_hybrid_verilator_like_cli.py`, and `tests/contract/test_public_pack_repeat_median_refresh_gates.py`. This is definition-only; it does not rewrite the index, commit changes, add measurement, change runtime/ABI, or claim native parser, arbitrary filelist, dependency inference, automatic allocation, resident optimization, GEM, production-serving, or raw full-state equality support.
- Commit split and public-pack cleanup review accepted: `config/scaling_gates/review_commit_split_and_public_pack_cleanup_gate.json` accepts the eight file-count groups as the operational split boundary but keeps index rewrite and commits blocked until the three per-file line guard risks are resolved or explicitly reviewed.
- Commit split line guard risks resolved: `config/scaling_gates/resolve_commit_split_line_guard_risks_gate.json` records that the guarded script and contract-test line limits pass under `python3 src/tools/check_staged_large_files.py --max-files 999`. The remaining blocker is file count, so the next task is rewriting the index into the accepted eight groups without using `git add -A`.
- Commit split index rewrite completed: `config/scaling_gates/execute_commit_split_index_rewrite_gate.json` records that the payload landed as eight hook-sized commits after the staged-only hook prerequisite. The largest payload commit touched `76` files, below the documented `100`-file guard, the worktree was clean, and `make simple && make check` passed with `221` contract tests. The next task is `define_verilator_native_option_parser_boundary_gate`; this adds no parser implementation, measurement, runtime/ABI change, arbitrary filelist support, dependency inference, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser boundary defined: `config/scaling_gates/define_verilator_native_option_parser_boundary_gate.json` defines the first native parser boundary as `verilator --cc --timing ... --sim-accel sidecar-gpu --sim-accel-states <N> --sim-accel-steps <S>` handing off to the existing sidecar plan contract. `--sim-accel-shape <NxS>`, target-first registry lookup, print modes, JSON operator-plan output, resident modes, and dataset-backed flows remain wrapper/shim compatibility for now. The next task is `review_verilator_native_option_parser_boundary_gate`; this adds no Verilator parser implementation, source patch, execution, measurement, runtime/ABI change, arbitrary filelist support, dependency inference, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_boundary_gate.json` accepts the parser-only/no-execution definition and records the weakest point: this repository still has no Verilator source tree or parser patch. The parser boundary also does not infer coverage manifests, host-probe metadata, source closure, state paths, or report paths. The next task is `run_verilator_native_option_parser_boundary_dry_run_gate`, limited to non-executing preview checks of the sidecar handoff contract.
- Verilator native option parser boundary dry-run passed: `config/scaling_gates/verilator_native_option_parser_boundary_dry_run_result_gate.json` records 12 non-executing preview/rejection commands. The two tracked `filelist_*` targets expose `64x1` sidecar handoff commands and `coverage_output_equivalence`; estimate mode remains a separate command. Missing shape halves, mixed shape spelling, and non-positive counts reject through the shared mapper, while unknown accelerators currently reject at argparse choices. The next task is `review_verilator_native_option_parser_boundary_dry_run_gate`; this adds no parser implementation, source patch, execution, measurement, runtime/ABI change, arbitrary filelist support, dependency inference, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser boundary dry-run review accepted: `config/scaling_gates/review_verilator_native_option_parser_boundary_dry_run_gate.json` accepts the 12-command non-executing result, but carries the validation-layer caveat forward: unknown accelerator names still reject in argparse choices, not the shared mapper. The next task is `define_verilator_native_option_parser_stub_boundary_gate`, which must define the parser-stub validation and handoff boundary before any native parser implementation claim.
- Verilator native option parser stub boundary defined: `config/scaling_gates/define_verilator_native_option_parser_stub_boundary_gate.json` assigns canonical unknown-accelerator validation to the parser-stub validation contract, keeps current argparse choices as wrapper compatibility only, and defines the structured handoff fields for future non-executing parser-stub fixtures. The next task is `review_verilator_native_option_parser_stub_boundary_gate`; this adds no parser implementation, source patch, execution, measurement, runtime/ABI change, arbitrary filelist support, dependency inference, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser stub boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_stub_boundary_gate.json` accepts that boundary narrowly and keeps the weak point explicit: the repository still has no Verilator source tree or parser patch, and observable unknown-accelerator rejection still comes through wrapper argparse choices. The next task is `define_verilator_native_option_parser_stub_fixture_gate`, limited to defining a non-executing fixture that makes the validation and handoff contract testable.
- Verilator native option parser stub fixture defined: `config/scaling_gates/define_verilator_native_option_parser_stub_fixture_gate.json` defines the non-executing importable fixture contract for accepting `sidecar-gpu` `64x1`, rejecting unknown accelerators outside wrapper argparse choices, rejecting missing/non-positive shape inputs, rejecting compact `--sim-accel-shape` as wrapper compatibility outside the native minimum, preserving ordinary Verilator args, and serializing parser-only handoff fields. The next task is `review_verilator_native_option_parser_stub_fixture_gate`; this adds no helper implementation, parser implementation, source patch, execution, measurement, runtime/ABI change, arbitrary filelist support, dependency inference, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser stub fixture review accepted: `config/scaling_gates/review_verilator_native_option_parser_stub_fixture_gate.json` accepts that fixture contract and advances to `implement_verilator_native_option_parser_stub_fixture_gate`. The implementation must be an importable non-executing helper, not a public CLI, and must make unknown-accelerator rejection observable outside wrapper argparse choices while preserving ordinary Verilator inputs and parser-only handoff fields.
- Verilator native option parser stub fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_stub_fixture_gate.json` records `src/tools/verilator_native_option_parser_stub_fixture.py` and `parse_verilator_native_option_stub`. The helper accepts expanded `--sim-accel sidecar-gpu --sim-accel-states 64 --sim-accel-steps 1`, rejects unknown accelerators through parser-stub validation while preserving the unsupported value, rejects compact and mixed compact shape spelling outside the native minimum, and preserves ordinary Verilator args without source-closure inference. The next task is `review_verilator_native_option_parser_stub_fixture_implementation_gate`; this adds no public CLI, native Verilator parser, source patch, execution, measurement, runtime/ABI change, arbitrary RTL/filelist support, dependency inference, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser stub fixture implementation review accepted: `config/scaling_gates/review_verilator_native_option_parser_stub_fixture_implementation_gate.json` accepts the helper narrowly and advances to `define_verilator_native_option_parser_source_patch_boundary_gate`. The review keeps the weak point explicit: this is still a Python fixture helper, not a Verilator source patch or complete Verilator option parser, and it does not add execution, measurement, source closure inference, target registry lookup, automatic allocation, or arbitrary RTL/filelist support.
- Verilator native option parser source-patch boundary defined: `config/scaling_gates/define_verilator_native_option_parser_source_patch_boundary_gate.json` selects a repo-owned overlay patch descriptor boundary, not a vendored Verilator source tree or submodule. The defined native minimum remains `--sim-accel sidecar-gpu --sim-accel-states <N> --sim-accel-steps <S>`, maps parser validation errors to the accepted parser-stub contract, and keeps build, execution, compare, source-closure inference, filelist expansion, and automatic allocation out of the parser layer. The next task is `review_verilator_native_option_parser_source_patch_boundary_gate`; this adds no patch file, native parser implementation, execution, measurement, runtime/ABI change, arbitrary RTL/filelist support, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser source-patch boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_source_patch_boundary_gate.json` accepts the overlay descriptor boundary and advances to `define_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate`. The next gate must pin the descriptor schema/path, upstream Verilator ref, touched upstream files, and a reproducible apply-check command before any source patch implementation claim; this adds no patch file, native parser implementation, execution, measurement, runtime/ABI change, arbitrary RTL/filelist support, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser overlay patch descriptor/apply-check boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate.json` defines the future descriptor path as `overlays/verilator/patches/verilator_native_option_parser_sidecar_gpu_v5_048.json`, the future patch file as `overlays/verilator/patches/verilator_native_option_parser_sidecar_gpu_v5_048.patch`, and pins upstream Verilator `v5.048` at `d0aa828c217410fffc73d92077b6f4f54830357c`. The first source touch candidates are `src/V3Options.h` and `src/V3Options.cpp`; shared option-parser mechanics, upstream docs/tests, compact shape spelling, and estimate-efficiency remain deferred unless the review expands scope. This adds no descriptor file, patch file, native parser implementation, execution, measurement, runtime/ABI change, arbitrary RTL/filelist support, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser overlay patch descriptor/apply-check review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate.json` accepts the pinned descriptor/apply-check boundary and advances to `implement_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate`. The next gate may add only the accepted descriptor and patch files plus source-of-truth/test/doc alignment; this adds no descriptor file, patch file, landed parser implementation, execution, measurement, runtime/ABI change, arbitrary RTL/filelist support, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.
- Verilator native option parser overlay patch descriptor/apply-check payload implemented: `config/scaling_gates/implement_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate.json` adds `overlays/verilator/patches/verilator_native_option_parser_sidecar_gpu_v5_048.json` and `overlays/verilator/patches/verilator_native_option_parser_sidecar_gpu_v5_048.patch`. The descriptor validates as JSON and the patch passes `git apply --check` against Verilator `v5.048` peeled commit `d0aa828c217410fffc73d92077b6f4f54830357c`. This is parse/store/validate-only overlay evidence and adds no native parser support claim, Verilator build/regression result, execution, measurement, runtime/ABI change, arbitrary RTL/filelist support, automatic allocation, GEM comparison, production-serving claim, or raw full-state equality claim.

## Source Of Truth

Canonical project state lives in:

- `config/README.md`
- `config/selection.json`
- `config/targets.json`
- `docs/status.md`
- `docs/roadmap.md`
- `README.md`

`docs/results.md` is the external-facing result pack and reader guide. It summarizes current evidence, reproduction commands, and non-claims, but canonical project-state decisions stay in the files above.

Historical gate details live under `records/scaling_gates/`, with `config/scaling_gates` kept as a compatibility link. Generated outputs are reproducible under `reports/` and `artifacts/`; they may be present in a local working tree as evidence, but they are never source of truth.

The config minimization audit is `records/scaling_gates/config_minimal_surface_completion_audit.json`.

## Operator shortcuts

`Makefile` targets are thin wrappers that call **existing** tools directly (no shared “do everything” Python script):

```bash
make simple          # status + validate + smoke (まずはこれ)
make status          # jq on config/selection.json (current_priority)
make validate        # jq empty on canonical config/*.json
make surface         # fast active-surface guard
make test            # python3 -m unittest discover -s tests/contract
make check           # validate, surface, then test
make smoke           # python3 src/tools/run_results_reproduction.py --dry-run
make mobile-vit-venv # venv + pip install -r requirements/mobile_vit.txt
make clean-mobile-vit-venv
make hooks           # print git hooksPath setup command
```

Equivalent direct commands (same behavior without `make`):

```bash
jq empty config/selection.json config/selection_extensions.json \
  config/selection_verification_commands.json config/targets.json
python3 -m unittest discover -s tests/contract -q
python3 src/tools/run_results_reproduction.py --dry-run
```

Merged selection state for tooling: `src/tools/selection_state.py` (`load_selection`).

Upstream RTL source is provided through submodules. Initialize the active upstream source before running target templates:

```bash
git submodule update --init third_party/rtlmeter
```

## Active Scope

**Target names** live in `config/targets.json` (`active_targets`). **`load_selection()`** injects `active_scope.targets` from that registry when you need the combined machine-readable view.

The compact `config/selection.json` still records scope metadata:

- `active_target_count`
- `active_tlul_target_count`
- `active_primitive_target_count`
- `latest_primitive`
- `coverage_output_equivalence_gate`
- `coverage_output_equivalence_report`
- strict output word and byte counts

Candidate targets are empty until the next gate is selected.

## What This Repo Can Do

- Build Verilator-derived CPU and hybrid host/GPU probes.
- Compare CPU vs hybrid by coverage-output equivalence rather than raw internal state.
- Preserve host-only Verilator internal handling before launch.
- Run slice launch templates such as TL-UL, OpenTitan primitive, NVDLA, ITA, MHA, paged KV-cache, and paged-attention KV-score seeds.
- Generate Verilator-like hybrid config from a target/top/overlay description.

## Minimal CPU/GPU Repro Flow

Existing template runner:

```bash
python3 src/tools/run_hybrid_template.py config/slice_launch_templates/pulp_ita_mha.json --shape 32x1
python3 src/tools/run_hybrid_template.py config/slice_launch_templates/pulp_ita_mha.json --shape 1x32
python3 src/tools/run_hybrid_template.py config/slice_launch_templates/pulp_paged_attention_kv_score.json --shape 64x1 --dry-run --estimate-efficiency
python3 src/tools/run_hybrid_template.py config/slice_launch_templates/pulp_paged_attention_kv_score.json --shape 64x1 --dry-run --estimate-efficiency-json
```

`--estimate-efficiency` prints a short human-readable `high` / `medium` / `low` estimate. `--estimate-efficiency-json` keeps the same data machine-readable for tests and wrappers. Existing CPU and hybrid reports can add an observed `cpu_to_hybrid_wall_speedup`, but this remains scoped evidence for that target/shape and is not a broad speedup claim.

Verilator-like benchmark runner:

```bash
python3 src/tools/run_hybrid_benchmark.py --list-targets sidecar_gpu
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel-shape 64x1 --print-operator-plan
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel-shape 64x1 --operator-plan-json
```

Those three commands are the shortest operator path: discover sidecar-ready targets, inspect the terminal command plus efficiency estimate, then emit the same non-executing plan as JSON for automation.

Full command reference:

```bash
python3 src/tools/run_hybrid_benchmark.py --list-targets
python3 src/tools/run_hybrid_benchmark.py --list-targets sidecar_gpu
python3 src/tools/run_hybrid_benchmark.py --help
python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 64x1 --dry-run
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --shape 64x1 --dry-run
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --shape 64x1 --dry-run --estimate-efficiency
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --shape 64x1 --dry-run --estimate-efficiency-json
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --shape 64x1 --dry-run --sidecar-gpu
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel sidecar-gpu --sim-accel-states 64 --sim-accel-steps 1 --dry-run
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel-shape 64x1 --print-verilator-command
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel-shape 64x1 --print-verilator-estimate-command
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel-shape 64x1 --print-efficiency-estimate
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel-shape 64x1 --print-operator-plan
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel-shape 64x1 --operator-plan-json
python3 src/tools/verilator_sidecar_shim.py --target paged_attention_kv_score --sim-accel sidecar-gpu --sim-accel-states 64 --sim-accel-steps 1
python3 src/tools/run_hybrid_benchmark.py pulp_paged_kv_cache_large --shape 256x1 --dry-run
python3 src/tools/run_hybrid_benchmark.py mobile_vit --limit 128 --dry-run
python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 16x64 --mode resident-state-reuse --dry-run
python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 16x64 --mode persistent-resident-state-abi --dry-run
python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 1x1 --summary-out reports/hybrid_benchmark_pulp_ita_mha_template_1x1.json
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --shape 1x1 --summary-out reports/hybrid_benchmark_paged_attention_kv_score_template_1x1.json
python3 src/tools/run_hybrid_benchmark.py pulp_paged_kv_cache_large --shape 1x1 --summary-out reports/hybrid_benchmark_pulp_paged_kv_cache_large_template_1x1.json
python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 1x1 --mode resident-state-reuse --phases 2 --summary-out reports/hybrid_benchmark_pulp_ita_mha_resident_state_reuse_1x1.json
python3 src/tools/run_hybrid_benchmark.py pulp_ita_mha --shape 1x1 --mode persistent-resident-state-abi --phases 2 --summary-out reports/hybrid_benchmark_pulp_ita_mha_persistent_resident_state_abi_1x1.json
python3 src/tools/run_hybrid_benchmark.py mobile_vit --limit 128 --summary-from-existing --summary-out reports/hybrid_benchmark_mobile_vit_template_limit128.json
```

Use `--list-targets` to inspect supported target names, aliases, required `--shape` or `--limit` arguments, supported modes, and the current `sidecar_gpu` option-shim discovery status before running a measurement. Use `--list-targets sidecar_gpu` for the focused sidecar discovery view referenced by operator-plan JSON discovery hints.

Use `--help` for the shortest Verilator-like entrypoint examples: target discovery, `--sidecar-gpu --dry-run`, `--sidecar-gpu --preflight`, terminal command preview, terminal estimate-command preview, terminal operator plan, and JSON operator plan.

The `--list-targets` sidecar discovery block also reports ready template stage names, not-ready resident fallback stage names, and dataset-backed stage names. The focused `--list-targets sidecar_gpu` view includes `shortest_operator_path`, matching the three commands above. For ready slice-template targets, it includes parameterized and concrete commands for terminal operator plans, estimate-command previews, and JSON operator plans, using the recommended `64x1` starting shape. These commands use `recommended_entrypoint: --sim-accel-shape <NxS>`; the expanded `compatibility_entrypoint` remains visible separately for tools that need the future direct-Verilator spelling. This keeps the first discovery command tied to the shim commands that can inspect or emit stage commands.

`run_hybrid_benchmark.py --estimate-efficiency` is the operator-facing form: it prints the target, shape or limit, speedup class, reason, next action, and non-claims after the command plan. `--estimate-efficiency-json` keeps the same estimate machine-readable. `--preflight` already emits JSON and cannot be combined with the extra efficiency output flags.

`--sidecar-gpu` is a short Verilator-like alias for the existing hybrid sidecar GPU benchmark flow. It does not change the CPU/GPU comparison policy; it adds the human-readable efficiency estimate so the operator can see the command plan, speedup class, and equivalence non-claims in one terminal view.

The wrapper also accepts `--sim-accel sidecar-gpu --sim-accel-states <N> --sim-accel-steps <S>` and compact `--sim-accel-shape <NxS>` as tested compatibility spellings for the planned Verilator option. Using a `--sim-accel-*` shape spelling enters the sidecar preview path even when `--sim-accel sidecar-gpu` is omitted. These names map to the same internal `NxS` shape and reject mixed shape spellings.

`--sim-accel-estimate-efficiency` on the wrapper follows the normal execution or `--dry-run` path, matching a future Verilator command-line flag that annotates a run with an estimate. Use `--print-efficiency-estimate` when you want the estimate-only, non-executing preview.

```bash
python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel-shape 64x1 --sim-accel-estimate-efficiency --dry-run
```

`--print-verilator-command` on the wrapper prints only the synthesized future Verilator command without executing commands. `--print-verilator-estimate-command` prints only the same command with `--sim-accel-estimate-efficiency` appended. `--print-efficiency-estimate` prints only the matching human-readable estimate. `--print-operator-plan` prints both command forms plus the same efficiency estimate. These are the shortest terminal paths from target discovery to the direct-option preview, while the JSON shim remains the structured automation boundary.

`--operator-plan-json` prints the same wrapper operator plan as machine-readable JSON without executing commands. It exits `0` with a `planned` report when ready, and exits `2` with a `not_ready_for_verilator_option_shim` JSON report when the target cannot synthesize the direct-option preview. The ready report includes `handoff_contract` metadata for state authority, init/reference/candidate dumps, generated compare report path, and `coverage_output_equivalence`; it also includes top-level `requested_compatibility_entrypoint`, the concrete expanded option suffix included in `command`. Treat the generated compare report through its selected `coverage_output_equivalence` policy, not raw final-state `match`; raw state equality may remain false while strict coverage-output words pass. The separate `estimate_command` appends `--sim-accel-estimate-efficiency`, keeping command-only previews clean while giving automation the future Verilator spelling for an estimate-annotated run. `discovery_hint` repeats the same requested-shape compatibility spelling plus the recommended `64x1` starting shape, `recommended_entrypoint`, `compatibility_entrypoint`, and terminal/JSON example commands exposed by `--list-targets`. `discovery_hint.requested_shape` and `recommended_shape_matches_request` keep the requested benchmark shape separate from the discovery starting point. Its `schema_role` is `target_first_operator_plan`; use `verilator_sidecar_shim.py` when automation needs the fuller readiness or stage-detail boundary.

Readiness status strings are shared across discovery and JSON entrypoints: `ready_for_template_shape`, `ready_for_verilator_option_shim`, and `not_ready_for_verilator_option_shim`.

`--preflight` includes a `sidecar_stage_plan` for template targets. It names the direct-Verilator migration stages, including `gpu_artifact_build`, `hybrid_sidecar_run`, and the final `coverage_output_compare` using `coverage_output_equivalence`. Each stage also carries structured `details` so `mdir`, `top_module`, source files, state files, shape, and compare policy are available without scraping command strings.

`sidecar_stage_plan.verilator_option_readiness` reports whether the wrapper has the minimum structured inputs for a future Verilator option shim. `ready_for_verilator_option_shim` is a planning/readiness claim only; it is not execution evidence and does not mean Verilator itself already implements `--sim-accel`.

Wrapper `--preflight` and summary JSON also include `verilator_option_preview`. Ready previews include the synthesized direct-option command, matching `estimate_command` with `--sim-accel-estimate-efficiency`, concrete `requested_compatibility_entrypoint`, and handoff contract; not-ready previews keep the missing input list without pretending Verilator already implements `--sim-accel`. Preflight and summary JSON also include the same `discovery_hint` as operator-plan JSON when a preview can be synthesized.

When `--dry-run` is used with `--sidecar-gpu` or explicit `--sim-accel sidecar-gpu`, the wrapper prints a compact `# verilator_option_preview` block before the existing wrapper command plan. It includes both command forms and the concrete requested compatibility entrypoint, keeping the future Verilator command visible while leaving the current dry-run and `coverage_output_equivalence` flow unchanged.

`--sidecar-gpu --preflight` is accepted as the JSON-only companion path. It includes `efficiency_estimate` and `verilator_option_preview` in the JSON report, while explicit terminal estimate output flags remain invalid with `--preflight`.

Preflight and summary JSON include `operator_entrypoint` so downstream tooling can tell whether the wrapper was invoked through `--sidecar-gpu`, explicit `--sim-accel`, or the ordinary target/shape surface. The entrypoint keeps the raw `sim_accel` value separate from `effective_sim_accel`, so compact `--sim-accel-shape` invocations still record the effective `sidecar-gpu` path even when the explicit selector was omitted.

Dataset-backed targets stay not-ready for the direct Verilator option until they have a direct RTL sidecar handoff. `mobile_vit --limit 128` still exposes non-executing `host_preprocess` and `rtl_sidecar_proxy_eval` stages so automation can inspect the host preprocessing boundary and the current RTL proxy eval command without treating either as shim readiness.

Resident modes also stay not-ready for the direct Verilator option. Their not-ready plan exposes a `resident_state_reuse_workflow` or `persistent_resident_state_abi_workflow` fallback command so low-efficiency `1xN` shapes can move to the supported resident workflow without implying Verilator owns that flow yet.

`src/tools/verilator_sidecar_shim.py` emits the future option handoff as JSON without executing commands. It exits `0` when ready for the shim, `2` when the target or mode is not ready for the shim, and `1` for input/planning errors with JSON on stderr. The shim accepts both expanded `--sim-accel-states <N> --sim-accel-steps <S>` and compact `--sim-accel-shape <NxS>` shape spellings. When the shim emits a synthesized Verilator command, it also includes the same `discovery_hint` as wrapper operator-plan JSON, both at the top level and inside `operator_plan`.

To inspect the exact command for one planned stage without running it:

```bash
python3 src/tools/verilator_sidecar_shim.py --target paged_attention_kv_score --sim-accel sidecar-gpu --sim-accel-states 64 --sim-accel-steps 1 --stage hybrid_sidecar_run --emit-command
```

The emitted command is still planning output; it is not execution evidence.

To preview the future direct Verilator command shape without running it:

```bash
python3 src/tools/verilator_sidecar_shim.py --target paged_attention_kv_score --sim-accel-shape 64x1 --emit-verilator-command
```

This synthesized command is a handoff preview for automation. It does not mean Verilator itself already implements `--sim-accel`.

For a one-line human-readable preview:

```bash
python3 src/tools/verilator_sidecar_shim.py --target paged_attention_kv_score --sim-accel sidecar-gpu --sim-accel-states 64 --sim-accel-steps 1 --print-verilator-command
```

For the matching human-readable efficiency estimate:

```bash
python3 src/tools/verilator_sidecar_shim.py --target paged_attention_kv_score --sim-accel-shape 64x1 --sim-accel-estimate-efficiency
```

`--sim-accel-estimate-efficiency` is the Verilator-compatible spelling for the shim's estimate-only terminal mode. It is equivalent to `--print-efficiency-estimate` on the current non-executing shim and remains separate from command-only output.

To see both the command preview and efficiency estimate in one terminal view:

```bash
python3 src/tools/verilator_sidecar_shim.py --target paged_attention_kv_score --sim-accel sidecar-gpu --sim-accel-states 64 --sim-accel-steps 1 --print-operator-plan
```

The target end state is a direct Verilator option, documented in `docs/verilator_sidecar_option.md`. The wrapper spelling above is the current compatibility surface while that option is not implemented in Verilator itself.

`--summary-from-existing` writes a wrapper summary from already generated reports without rerunning benchmark commands.

Generate a new hybrid config:

```bash
python3 src/tools/gen_hybrid_config.py \
  --target PULP_ITA.my_target \
  --top-module my_target_gpu_cov_tb \
  --overlay overlays/my/src/my_target_gpu_cov_tb.sv \
  --source-closure-from-template config/slice_launch_templates/filelist_paged_attention_kv_score.json \
  --dry-run
```

The generated config covers the scaling gate, coverage manifest, slice launch template, and generic host-probe build metadata. New generated templates use `src/tools/build_host_probe.py`, so they do not require adding a matching target to `src/hybrid/Makefile`. `--source-closure-from-template` only copies from a tracked template that already declares `source_closure.status=complete`; it is not arbitrary RTL dependency inference.

## Coverage Output Equivalence

Acceptance policy:

```text
coverage_output_equivalence
```

The comparison target is the declared output word set, not raw Verilator state. This keeps CPU vs hybrid comparison stable when host-only internal fields differ.

Representative evidence:

- `reports/pulp_ita_mha_cpu_vs_hybrid_1x1_coverage_output_compare.json`
- `reports/pulp_ita_mha_cpu_vs_hybrid_32x1_coverage_output_compare.json`
- `reports/pulp_ita_mha_cpu_vs_hybrid_1x32_coverage_output_compare.json`
- `reports/pulp_paged_kv_cache_large_first_hybrid_benchmark_summary.json`
- `reports/pulp_ita_mha_first_hybrid_benchmark_summary.json`
- `reports/pulp_ita_mha_64x1_1x64_scaling_summary.json`
- `reports/pulp_ita_mha_prefill_decode_split_summary.json`
- `reports/pulp_ita_mha_resident_decode_1x64_summary.json`
- `reports/pulp_ita_mha_resident_decode_batch_parallel_summary.json`
- `reports/pulp_paged_attention_kv_score_first_hybrid_benchmark_summary.json`
- `reports/results_reproduction_median_summary.json`
- `reports/resident_batch_sweep_summary.json`
- `reports/resident_state_reuse_experiment_summary.json`
- `reports/persistent_resident_state_abi_probe_summary.json`

## Completed NN Gates

Current goal evidence includes:

- `neural_network_rtl_paged_kv_cache_large_scaleup_gate.json`
- `neural_network_rtl_paged_kv_cache_large_review_gate.json`
- `neural_network_rtl_full_ita_mha_dependency_audit_gate.json`
- `neural_network_rtl_full_ita_mha_first_hybrid_benchmark_gate.json`
- `neural_network_rtl_full_ita_mha_larger_paged_kv_goal_review_gate.json`
- `neural_network_rtl_paged_attention_kv_score_harness_gate.json`
- `full_ita_mha_larger_paged_attention_kv_goal_completion_audit.json`
- `hybrid_verilator_like_config_and_probe_generation_gate.json`
- `full_mha_hybrid_try_completion_audit.json`
- `full_mha_scaleup_64x1_1x64_gate.json`
- `prefill_decode_split_mha_benchmark_gate.json`
- `resident_decode_optimization_probe_gate.json`
- `resident_decode_batch_parallel_probe_gate.json`
- `modern_llm_serving_rtl_hybrid_conditions_goal_completion_audit.json`
- `public_results_packaging_gate.json`
- `public_benchmark_pack_externalization_readiness_audit.json`
- `public_results_packaging_refresh_after_pulp_ita_mha_shape_expansion_gate.json`
- `public_results_packaging_refresh_after_persistent_resident_repeat_median_gate.json`
- `public_benchmark_pack_externalization_completion_gate.json`
- `next_measurement_selection_after_public_benchmark_pack_externalization_gate.json`
- `paged_attention_kv_cache_scale_up_measurement_gate.json`
- `pulp_ita_mha_first_generic_host_probe_build_run_compare_gate.json`
- `pulp_ita_mha_shape_expansion_gate.json`
- `pulp_ita_mha_shape_expansion_review_gate.json`
- `public_benchmark_pack_goal_completion_audit.json`
- `one_command_reproduction_flow_gate.json`
- `repeat_median_results_reproduction_gate.json`
- `resident_execution_optimization_next_gate.json`
- `resident_batch_sweep_measurement_gate.json`
- `resident_batch_sweep_review_gate.json`
- `resident_state_reuse_experiment_gate.json`
- `resident_state_reuse_measurement_gate.json`
- `resident_state_reuse_review_gate.json`
- `persistent_resident_state_abi_probe_gate.json`
- `persistent_resident_state_abi_probe_implementation_gate.json`
- `persistent_resident_device_handle_storage_gate.json`
- `persistent_resident_device_handle_storage_review_gate.json`

Relevant RTL/harness surface:

- `overlays/ITA/src/pulp_paged_kv_cache_large_gpu_cov_tb.sv`
- `pulp_paged_kv_cache_large_host_probe`
- `tc_sram`

## MobileViT CPU-Kick Evidence

MobileViT is kept as CPU-kick evidence, not as RTL equivalence proof:

- `mobile_vit_cpu_kick_imagenet_accuracy`
- `mobile_vit_cpu_kick_rtl_hybrid_boundary`
- `apple/mobilevit-small`
- `complete_full_imagenet_validation_cpu_kick_accuracy_measured`
- `top-1 0.77022`
- `mobile_vit_cpu_kick_rtl_proxy_host_probe`
- `mobile_vit_hybrid_imagenet_eval_gate`
- `mobile_vit_hybrid_imagenet_eval_completion_audit`
- `mobile_vit_hybrid_imagenet_limit_128_scaleup_completion_audit`

Hybrid ImageNet evaluation entrypoint:

```bash
python3 -m venv artifacts/mobile_vit/venv
artifacts/mobile_vit/venv/bin/python -m pip install -U pip
artifacts/mobile_vit/venv/bin/python -m pip install -r requirements/mobile_vit.txt

python3 src/tools/run_results_reproduction.py --mobile-vit-imagenet-128 --dry-run
python3 src/tools/run_results_reproduction.py --mobile-vit-imagenet-128
```

This regenerates the `limit 128` manifest, runs CPU-kick MobileViT inference, runs the `mobile_vit_cpu_kick_rtl_proxy` hybrid control-boundary batch, and computes accuracy from CPU-kick predictions. It is not a claim that MobileViT logits are produced by RTL.

Current retained evidence includes both the real cached two-image smoke and a `limit 128` local-cache ImageNet scale-up:

- manifest: `artifacts/mobile_vit/apple_mobilevit_small/imagenet_manifest_128.json`
- summary: `reports/mobile_vit_hybrid_128_summary.json`
- compare: `reports/mobile_vit_cpu_kick_rtl_proxy_imagenet_batch1_128_cpu_vs_hybrid_1x1_coverage_output_compare.json`
- result: `evaluated_count=128`, `top1_accuracy=0.7734375`, `top5_accuracy=0.953125`, `coverage_output_equivalence_complete=true`

Full 50k ImageNet hybrid scale-up remains optional follow-up work.

## Artifact Policy

Ignored generated outputs:

- `artifacts/`
- `reports/`
- `work/`

Do not put canonical decisions in generated outputs. If a generated result matters, record the regeneration command in docs and reference the generated path from the relevant gate. `reports/` and `artifacts/` are allowed to be empty except for `.gitignore`.

### Local disk hygiene (optional)

These paths are **regenerable**; remove them when reclaiming disk, then follow the matching commands in this README, `docs/results.md`, or the relevant gate JSON.

- **`artifacts/mobile_vit/venv`**: GPU-oriented PyTorch / Hugging Face stack used by `src/tools/results_reproduction_mobile_vit.py` (`MOBILE_VIT_PYTHON`). Recreate with the **MobileViT CPU-Kick Evidence** `venv` + `pip install` block above.
- **Verilator `artifacts/*_obj_dir/**`**: per-target `--Mdir` trees. Delete DUTs you no longer build; recreate via the template / `run_hybrid_template.py` / gate-documented `verilator --cc ... --Mdir ...` flows.

## Next Direction

The paged-attention/KV-cache repeat-median public-pack boundary is complete. The selected next workstream is `config_generation_validation_breadth`:

- use `config/scaling_gates/next_measurement_selection_after_paged_attention_kv_cache_repeat_median_refresh_gate.json` as the source selection gate for the config-generation validation breadth workstream
- use `config/scaling_gates/public_benchmark_pack_externalization_completion_after_paged_attention_kv_cache_repeat_median_gate.json` as the source completion gate
- use `config/scaling_gates/public_results_packaging_refresh_after_paged_attention_kv_cache_timing_summary_gate.json` as the source refresh gate
- use `config/scaling_gates/next_measurement_selection_after_public_benchmark_pack_externalization_gate.json` as the source selection gate
- use `config/scaling_gates/public_benchmark_pack_externalization_readiness_audit.json` as the externalization readiness audit
- use `config/scaling_gates/public_benchmark_pack_externalization_completion_gate.json` as the source completion gate
- use `config/scaling_gates/next_goal_selection_after_persistent_resident_repeat_median_gate.json` as the current next-goal selection gate after the repeat-median result
- use `docs/results.md` as the external-facing benchmark pack
- use `config/scaling_gates/public_results_packaging_refresh_after_pulp_ita_mha_shape_expansion_gate.json` for the fresh MHA `1x1`, `32x1`, and `1x32` generic-host-probe chain
- use `reports/persistent_resident_state_abi_repeat_median_summary.json` as the newest persistent resident ABI timing evidence
- keep `coverage_output_equivalence` as the correctness policy
- run `python3 src/tools/run_results_reproduction.py --persistent-resident-state-abi 16x64 --persistent-resident-state-abi-phases 4 --persistent-resident-state-abi-repeat-median 3 --dry-run` to inspect the persistent resident repeat-median flow
- run `python3 src/tools/run_results_reproduction.py --persistent-resident-state-abi 16x64 --persistent-resident-state-abi-phases 4 --persistent-resident-state-abi-repeat-median 3` to regenerate the report; the current median hybrid wall is `4.965 ms`, median GPU kernel total is `4.934624 ms`, and all samples pass coverage-output equivalence with mismatch count `0`
- current selected goal is complete: the public benchmark pack externalization boundary is closed without changing runtime or ABI behavior
- dry-run completed: all four paged-attention/KV-cache scale-up dry-run commands exited with code `0`
- measurement completed: all four paged-attention/KV-cache scale-up measurement commands exited with code `0` and passed `coverage_output_equivalence` with mismatch count `0`
- generated compare evidence: `reports/pulp_paged_kv_cache_large_cpu_vs_hybrid_256x1_coverage_output_compare.json`, `reports/pulp_paged_kv_cache_large_cpu_vs_hybrid_1x64_coverage_output_compare.json`, `reports/pulp_paged_attention_kv_score_cpu_vs_hybrid_64x1_coverage_output_compare.json`, and `reports/pulp_paged_attention_kv_score_cpu_vs_hybrid_1x64_coverage_output_compare.json`
- review completed: `config/scaling_gates/paged_attention_kv_cache_scale_up_measurement_review_gate.json` selects a timing summary gate because the compare reports prove correctness but do not contain timing fields
- timing summary defined: `config/scaling_gates/paged_attention_kv_cache_timing_summary_gate.json` records single-run CPU, hybrid wall, and GPU-kernel timing for the same four shapes without changing runtime, ABI, or workload scope
- timing summary reviewed: `config/scaling_gates/paged_attention_kv_cache_timing_summary_review_gate.json` selects a public-results packaging refresh next, while preserving the single-run and non-production boundaries
- public results refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_paged_attention_kv_cache_timing_summary_gate.json` keeps the reviewed four-shape correctness and single-run timing evidence in the public pack without adding a measurement, workload, runtime change, repeat-median claim, or production serving claim
- public benchmark pack externalization completed after timing refresh: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_paged_attention_kv_cache_timing_summary_gate.json` closes the publication boundary before selecting another measurement
- next measurement selected: `config/scaling_gates/next_measurement_selection_after_paged_attention_kv_cache_timing_refresh_gate.json` selects repeat-median timing for the existing four-shape paged-attention/KV-cache set, because the remaining evidence gap is timing stability rather than another single-run shape expansion
- repeat-median timing boundary defined: `config/scaling_gates/paged_attention_kv_cache_repeat_median_timing_gate.json` fixes the existing four-shape set and records that the existing `--repeat-median` workflow does not cover all four required paged-attention/KV-cache shapes
- repeat-median workflow added: `config/scaling_gates/paged_attention_kv_cache_repeat_median_workflow_gate.json` adds `python3 src/tools/run_results_reproduction.py --paged-kv-repeat-median 3 --dry-run` for all four reviewed paged-attention/KV-cache shapes
- repeat-median measurement recorded: `config/scaling_gates/paged_attention_kv_cache_repeat_median_measurement_gate.json` records `reports/paged_attention_kv_cache_repeat_median_summary.json`; all four shapes pass `coverage_output_equivalence` with mismatch count `0`
- repeat-median review completed: `config/scaling_gates/paged_attention_kv_cache_repeat_median_review_gate.json` accepts the scoped repeat-count `3` measurement for the public pack and keeps production serving, raw full-state equality, runtime/ABI, and new-workload claims out of scope
- repeat-median public results refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_paged_attention_kv_cache_repeat_median_gate.json` keeps the reviewed repeat-median evidence in the public pack without adding a measurement, workload, runtime change, or production serving claim
- public benchmark pack externalization completed after repeat-median refresh: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_paged_attention_kv_cache_repeat_median_gate.json` closes the publication boundary before selecting another measurement
- next measurement selected after repeat-median refresh: `config/scaling_gates/next_measurement_selection_after_paged_attention_kv_cache_repeat_median_refresh_gate.json` selects `config_generation_validation_breadth`
- config-generation validation breadth defined: `config/scaling_gates/config_generation_validation_breadth_gate.json` fixes the tracked template set for generic host-probe metadata validation
- config-generation validation breadth dry-run passed: `config/scaling_gates/config_generation_validation_breadth_dry_run_gate.json` records six tracked `1x1 --dry-run` command plans using `src/tools/build_host_probe.py`
- config-generation validation breadth dry-run reviewed: `config/scaling_gates/config_generation_validation_breadth_dry_run_review_gate.json` accepts command-plan evidence and keeps build/run/compare claims out of scope
- config-generation validation breadth execution defined: `config/scaling_gates/config_generation_validation_breadth_execution_gate.json` fixes the six tracked `1x1` build/run/compare commands for the next execution gate
- config-generation validation breadth execution completed and reviewed: `config/scaling_gates/config_generation_validation_breadth_execution_result_gate.json` and `config/scaling_gates/config_generation_validation_breadth_execution_review_gate.json` record that all six tracked `1x1` non-dry-run commands passed `coverage_output_equivalence` with mismatch count `0`
- config-generation validation breadth public refresh completed: `config/scaling_gates/public_results_packaging_refresh_after_config_generation_validation_breadth_execution_gate.json`, `config/scaling_gates/public_benchmark_pack_externalization_completion_after_config_generation_validation_breadth_execution_gate.json`, and `config/scaling_gates/next_measurement_selection_after_config_generation_validation_breadth_public_pack_refresh_gate.json` close the publication boundary and select `define_config_generation_validation_shape_breadth_gate`
- config-generation validation shape breadth defined: `config/scaling_gates/config_generation_validation_shape_breadth_gate.json` selects the same six templates at `32x1` and `1x32`; the next step is dry-run command-plan validation
- config-generation validation shape breadth dry-run reviewed: `config/scaling_gates/config_generation_validation_shape_breadth_dry_run_gate.json` and `config/scaling_gates/config_generation_validation_shape_breadth_dry_run_review_gate.json` record that all 12 command plans pass and select `define_config_generation_validation_shape_breadth_execution_gate`
- config-generation validation shape breadth execution defined: `config/scaling_gates/config_generation_validation_shape_breadth_execution_gate.json` fixes the next real build/run/compare boundary as all 12 reviewed `32x1` / `1x32` commands and selects `run_config_generation_validation_shape_breadth_execution_gate`
- config-generation validation shape breadth execution completed and reviewed: `config/scaling_gates/config_generation_validation_shape_breadth_execution_result_gate.json` and `config/scaling_gates/config_generation_validation_shape_breadth_execution_review_gate.json` record that all 12 non-dry-run commands passed `coverage_output_equivalence` with mismatch count `0`; raw full-state equality remains false and timing/speedup claims remain out of scope
- config-generation validation shape breadth public refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_config_generation_validation_shape_breadth_execution_gate.json` admits the 12-command result into the public pack without adding timing, speedup, raw-state, runtime/ABI, or production-serving claims
- config-generation validation shape breadth public pack completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_config_generation_validation_shape_breadth_execution_gate.json` closes the publication boundary, and `config/scaling_gates/next_measurement_selection_after_config_generation_validation_shape_breadth_public_pack_refresh_gate.json` selects `define_config_generation_validation_additional_targets_gate`
- config-generation validation additional targets defined: `config/scaling_gates/config_generation_validation_additional_targets_gate.json` selects `nvdla_cmac_a2cacc`, `prim_count`, and `prim_secded_inv_39_32_enc` at `1x1 --dry-run` first
- config-generation validation additional targets dry-run reviewed: `config/scaling_gates/config_generation_validation_additional_targets_dry_run_gate.json` and `config/scaling_gates/config_generation_validation_additional_targets_dry_run_review_gate.json` record that all three command plans pass and select `define_config_generation_validation_additional_targets_execution_gate`
- config-generation validation additional targets execution defined: `config/scaling_gates/config_generation_validation_additional_targets_execution_gate.json` fixes the three reviewed `1x1` build/run/compare commands and selects `run_config_generation_validation_additional_targets_execution_gate`
- config-generation validation additional targets execution completed and reviewed: `config/scaling_gates/config_generation_validation_additional_targets_execution_result_gate.json` and `config/scaling_gates/config_generation_validation_additional_targets_execution_review_gate.json` record that all three `1x1` non-dry-run commands passed `coverage_output_equivalence` with mismatch count `0`; raw full-state equality remains false and timing/speedup claims remain out of scope
- config-generation validation additional targets public refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_config_generation_validation_additional_targets_execution_gate.json` admits the three-command result into the public pack without adding timing, speedup, raw-state, runtime/ABI, or production-serving claims
- config-generation validation additional targets public pack completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_config_generation_validation_additional_targets_execution_gate.json` closes the publication boundary, and `config/scaling_gates/next_measurement_selection_after_config_generation_validation_additional_targets_public_pack_refresh_gate.json` selects `define_config_generation_validation_followup_gate`
- config-generation validation follow-up selected: `config/scaling_gates/config_generation_validation_followup_gate.json` closes this generated-config correctness branch for now and selects `define_tlul_template_schema_metadata_invariant_review_gate`
- TL-UL/template metadata invariant review defined: `config/scaling_gates/tlul_template_schema_metadata_invariant_review_gate.json` fixes representative templates and required metadata invariants for generic host-probe plus `coverage_output_equivalence` planning; next is dry-run validation
- TL-UL/template metadata invariant review dry-run accepted: `config/scaling_gates/tlul_template_schema_metadata_invariant_review_dry_run_gate.json` and `config/scaling_gates/tlul_template_schema_metadata_invariant_review_dry_run_review_gate.json` record that all six representative dry-run plans pass and select execution definition
- TL-UL/template metadata invariant execution defined: `config/scaling_gates/tlul_template_schema_metadata_invariant_execution_gate.json` selects all six representative `1x1` non-dry-run build/run/compare commands and advances to `run_tlul_template_schema_metadata_invariant_execution_gate`
- TL-UL/template metadata invariant execution completed and reviewed: `config/scaling_gates/tlul_template_schema_metadata_invariant_execution_result_gate.json` and `config/scaling_gates/tlul_template_schema_metadata_invariant_execution_review_gate.json` record that all six representative `1x1` non-dry-run commands passed `coverage_output_equivalence` with mismatch count `0`; raw full-state equality remains false and public packaging refresh is next
- TL-UL/template metadata invariant public pack completed: `config/scaling_gates/public_results_packaging_refresh_after_tlul_template_schema_metadata_invariant_execution_gate.json`, `config/scaling_gates/public_benchmark_pack_externalization_completion_after_tlul_template_schema_metadata_invariant_execution_gate.json`, and `config/scaling_gates/next_measurement_selection_after_tlul_template_schema_metadata_invariant_public_pack_refresh_gate.json` close the publication boundary and select `define_verilator_like_hybrid_entrypoint_option_surface_gate`
- Verilator-like hybrid entrypoint surface defined: `config/scaling_gates/verilator_like_hybrid_entrypoint_option_surface_gate.json` selects target-name plus `--sim-accel-shape <NxS>` as the first operator-facing bridge toward a future Verilator option; raw filelist input is deferred until top/clock/reset/manifest inference is explicitly defined
- Verilator-like hybrid entrypoint surface dry-run reviewed: `config/scaling_gates/verilator_like_hybrid_entrypoint_option_surface_dry_run_gate.json` and `config/scaling_gates/verilator_like_hybrid_entrypoint_option_surface_dry_run_review_gate.json` record that six preview/operator-plan commands exit `0`, expose the target-name plus `--sim-accel-shape` surface, and select `define_verilator_like_hybrid_entrypoint_option_surface_execution_gate`; this remains non-executing plan evidence, not timing, speedup, or Verilator-native option evidence
- Verilator-like hybrid entrypoint execution defined: `config/scaling_gates/verilator_like_hybrid_entrypoint_option_surface_execution_gate.json` selects one non-dry-run operator command, `python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --sim-accel-shape 64x1 --summary-out reports/hybrid_benchmark_paged_attention_kv_score_sim_accel_64x1.json`, and requires `coverage_output_equivalence` on `reports/pulp_paged_attention_kv_score_cpu_vs_hybrid_64x1_coverage_output_compare.json`; this is definition-only until the command is run
- Verilator-like hybrid entrypoint execution completed and reviewed: `config/scaling_gates/verilator_like_hybrid_entrypoint_option_surface_execution_result_gate.json` and `config/scaling_gates/verilator_like_hybrid_entrypoint_option_surface_execution_review_gate.json` record that the non-dry-run operator command exited `0` and passed `coverage_output_equivalence` with mismatch count `0` over `29` words / `116` bytes; raw strict final-state equality remains false, and public packaging refresh is next
- Verilator-like hybrid entrypoint public pack completed: `config/scaling_gates/public_results_packaging_refresh_after_verilator_like_hybrid_entrypoint_option_surface_execution_gate.json`, `config/scaling_gates/public_benchmark_pack_externalization_completion_after_verilator_like_hybrid_entrypoint_option_surface_execution_gate.json`, and `config/scaling_gates/next_measurement_selection_after_verilator_like_hybrid_entrypoint_option_surface_public_pack_refresh_gate.json` close the publication boundary and select `define_filelist_to_verilator_like_hybrid_plan_boundary_gate`; this next gate should define a filelist-facing plan boundary without claiming arbitrary RTL support, optimal GPU allocation, or native Verilator integration
- Filelist-facing hybrid plan boundary defined: `config/scaling_gates/filelist_to_verilator_like_hybrid_plan_boundary_gate.json` treats repeatable `--source` inputs plus explicit top/overlay/clock/reset metadata as the first filelist-facing plan-only contract and selects `run_filelist_to_verilator_like_hybrid_plan_boundary_dry_run_gate`; this is not arbitrary RTL execution, automatic design inference, optimal GPU allocation, or native Verilator integration
- Filelist-facing hybrid plan dry-run reviewed: `config/scaling_gates/filelist_to_verilator_like_hybrid_plan_boundary_dry_run_gate.json` and `config/scaling_gates/filelist_to_verilator_like_hybrid_plan_boundary_dry_run_review_gate.json` record one accepted `gen_hybrid_config.py --source ... --overlay ... --dry-run` plan and one missing source/overlay refusal; the next gate decides whether generated manifest/template/gate drafts may be materialized as tracked source
- Filelist-facing hybrid plan materialization defined: `config/scaling_gates/filelist_to_verilator_like_hybrid_plan_boundary_materialization_gate.json` allows exactly three new tracked source files to be written from the reviewed dry-run payload and forbids overwriting existing hand-authored files; the next gate performs that write and validates JSON/metadata only
- Filelist-facing hybrid plan materialization completed and reviewed: `config/scaling_gates/filelist_to_verilator_like_hybrid_plan_boundary_materialization_result_gate.json` and `config/scaling_gates/filelist_to_verilator_like_hybrid_plan_boundary_materialization_review_gate.json` record that exactly three JSON source files were written for `filelist_paged_attention_kv_score`
- Filelist materialized template dry-run completed and reviewed: `config/scaling_gates/filelist_materialized_template_dry_run_result_gate.json` and `config/scaling_gates/filelist_materialized_template_dry_run_review_gate.json` record that `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/filelist_paged_attention_kv_score.json --shape 1x1 --dry-run` exited `0` and emitted the expected seven-stage command plan; the next gate defines the matching non-dry-run build/run/compare boundary
- Filelist materialized template execution defined: `config/scaling_gates/filelist_materialized_template_execution_gate.json` selects `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/filelist_paged_attention_kv_score.json --shape 1x1` as the next non-dry-run build/run/compare command and requires later `coverage_output_equivalence` mismatch count `0`
- Filelist materialized template execution completed and reviewed: `config/scaling_gates/filelist_materialized_template_execution_result_gate.json` records the initial source-closure failure, the template source-list repair, and the successful rerun. `config/scaling_gates/filelist_materialized_template_execution_review_gate.json` accepts only scoped `1x1` materialized-template execution with `coverage_output_equivalence` mismatch count `0`; the next gate refreshes public packaging without claiming arbitrary RTL, timing, speedup, native Verilator support, or optimal GPU allocation.
- Filelist materialized template public-pack refresh completed and reviewed: `config/scaling_gates/public_results_packaging_refresh_after_filelist_materialized_template_execution_result_gate.json` adds the scoped filelist gate chain, generated template/coverage manifest, generator helpers, and optional compare report to the public-pack manifest. `config/scaling_gates/public_results_packaging_refresh_after_filelist_materialized_template_execution_review_gate.json` accepts this refresh and selects the public-pack completion boundary next.
- Filelist materialized template public-pack completion and next selection completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_materialized_template_execution_gate.json` closes the externalization boundary, and `config/scaling_gates/next_measurement_selection_after_filelist_materialized_template_public_pack_refresh_gate.json` selects `define_filelist_source_closure_dependency_policy_gate` to address the source-closure weakness exposed by the first non-dry-run attempt.
- Filelist source-closure dependency policy defined: `config/scaling_gates/filelist_source_closure_dependency_policy_gate.json` requires explicit complete source closure for execution, allows copying a known tracked template source closure, and keeps arbitrary RTL dependency inference, native Verilator support, and automatic optimal GPU allocation as non-claims. The next task is a dry-run policy validation.
- Filelist source-closure dependency policy dry-run completed and reviewed: `config/scaling_gates/filelist_source_closure_dependency_policy_dry_run_result_gate.json` records that `ita.sv` plus overlay is marked `incomplete`, while the explicit 29-file ITA/common_cells closure is marked `complete`. `config/scaling_gates/filelist_source_closure_dependency_policy_dry_run_review_gate.json` accepts the dry-run metadata boundary and selects `define_filelist_source_closure_non_dry_run_refusal_gate` next.
- Filelist source-closure non-dry-run refusal completed and reviewed: `config/scaling_gates/filelist_source_closure_non_dry_run_refusal_result_gate.json` records that templates explicitly marked `source_closure.status=incomplete` are refused before Verilator on non-dry-run execution, while dry-run remains plan-only and the repaired tracked filelist template records a complete copied closure. `config/scaling_gates/filelist_source_closure_non_dry_run_refusal_review_gate.json` accepts this guard and selects public-pack refresh next.
- Filelist source-closure refusal public-pack refresh completed and reviewed: `config/scaling_gates/public_results_packaging_refresh_after_filelist_source_closure_refusal_result_gate.json` adds the source-closure policy/dry-run/refusal gate chain and the source-closure contract tests to the public-pack manifest. `config/scaling_gates/public_results_packaging_refresh_after_filelist_source_closure_refusal_review_gate.json` accepts this packaging-only refresh and selects public benchmark pack completion next.
- Filelist source-closure refusal public-pack completion and next selection completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_source_closure_refusal_gate.json` closes the packaging boundary, and `config/scaling_gates/next_measurement_selection_after_filelist_source_closure_refusal_public_pack_refresh_gate.json` selects `define_filelist_known_template_source_closure_copy_gate` next. The next workstream addresses the remaining operator burden of repeating long complete source lists by defining reviewed source-closure copy from a known tracked template.
- Filelist known-template source-closure copy dry-run completed and reviewed: `config/scaling_gates/filelist_known_template_source_closure_copy_gate.json`, `config/scaling_gates/filelist_known_template_source_closure_copy_dry_run_result_gate.json`, and `config/scaling_gates/filelist_known_template_source_closure_copy_dry_run_review_gate.json` define `gen_hybrid_config.py --source-closure-from-template`, require a tracked complete reference template, record `reference_template` and `source_files_sha256`, and refuse requested sources missing from the copied closure. The next workstream is materialization; this is still not arbitrary RTL dependency inference, native Verilator support, timing, speedup, or build/run/compare evidence.
- Filelist known-template source-closure copy materialization defined: `config/scaling_gates/filelist_known_template_source_closure_copy_materialization_gate.json` permits only the generated coverage manifest, slice launch template, and scaling gate paths to be materialized, requires copied complete source-closure metadata to remain visible, and makes default writes refuse existing source-of-truth files. The next task is the actual materialization run; this definition is not build/run/compare, timing, speedup, arbitrary dependency inference, native Verilator, or optimal GPU allocation evidence.
- Filelist known-template source-closure copy materialization completed and reviewed: `config/scaling_gates/filelist_known_template_source_closure_copy_materialization_result_gate.json` records exactly three written JSON payloads for `filelist_known_template_paged_attention_kv_score`; `config/scaling_gates/filelist_known_template_source_closure_copy_materialization_review_gate.json` accepts this as materialized source evidence only and selects the materialized-template dry-run boundary next.
- Filelist known-template source-closure copy materialized-template dry-run completed and reviewed: `config/scaling_gates/filelist_known_template_source_closure_copy_materialized_template_dry_run_result_gate.json` records a seven-stage `run_hybrid_template.py --dry-run` command plan for `filelist_known_template_paged_attention_kv_score`; `config/scaling_gates/filelist_known_template_source_closure_copy_materialized_template_dry_run_review_gate.json` accepts only command-plan evidence and selects the non-dry-run execution definition next.
- Filelist known-template source-closure copy materialized-template execution completed and reviewed: `config/scaling_gates/filelist_known_template_source_closure_copy_materialized_template_execution_result_gate.json` records `1x1` non-dry-run build/run/compare for `filelist_known_template_paged_attention_kv_score`. The selected `coverage_output_equivalence` policy passed with mismatch count `0` over `29` words / `116` bytes; raw final-state equality remains false with Verilator-internal-only mismatch bytes. `config/scaling_gates/filelist_known_template_source_closure_copy_materialized_template_execution_review_gate.json` accepts this scoped execution evidence and selects public-pack refresh next.
- Filelist known-template source-closure copy execution public-pack refresh completed and reviewed: `config/scaling_gates/public_results_packaging_refresh_after_filelist_known_template_source_closure_copy_execution_result_gate.json` confirms the known-template copy gate chain, materialized template/manifest, first benchmark gate, execution review gates, and optional compare report are included by the public archive dry-run. `config/scaling_gates/public_results_packaging_refresh_after_filelist_known_template_source_closure_copy_execution_review_gate.json` accepts this packaging-only refresh and selects externalization completion next.
- Filelist known-template source-closure copy execution externalization completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_known_template_source_closure_copy_execution_gate.json` closes the public-pack boundary for the selected `1x1` copied-closure execution. `config/scaling_gates/next_measurement_selection_after_filelist_known_template_source_closure_copy_execution_public_pack_refresh_gate.json` selects `define_known_template_source_closure_reference_inventory_gate` next because only the paged-attention filelist templates currently declare complete source-closure references.
- Known-template source-closure independent reference promotion reviewed: `config/scaling_gates/known_template_source_closure_independent_reference_promotion_review_gate.json` accepts `PULP_ITA.pulp_ita_mha` as the first promoted independent complete-reference template. `config/scaling_gates/next_measurement_selection_after_known_template_source_closure_independent_reference_promotion_review_gate.json` selects breadth validation next, while keeping arbitrary RTL dependency inference, native Verilator support, timing, speedup, and optimal GPU allocation out of scope.
- Known-template source-closure copy breadth validation defined: `config/scaling_gates/known_template_source_closure_copy_breadth_validation_gate.json` fixes two accepted dry-run copy cases, one paged-attention copied-lineage regression and one independent `PULP_ITA.pulp_ita_mha` reference copy, plus one requested-source refusal case. The next task is dry-run execution only; materialization, build/run/compare, timing, speedup, arbitrary dependency inference, native Verilator support, and optimal GPU allocation remain out of scope.
- Known-template source-closure copy breadth dry-run result: `config/scaling_gates/known_template_source_closure_copy_breadth_validation_dry_run_result_gate.json` records that both accepted dry-runs exited `0`, emitted complete copied source closures with expected reference templates and source-file hashes, and kept `src/tools/build_host_probe.py` plus `coverage_output_equivalence` planning. The requested-source-absent refusal exited `1`. This is still dry-run evidence only.
- Known-template source-closure copy breadth dry-run reviewed: `config/scaling_gates/known_template_source_closure_copy_breadth_validation_dry_run_review_gate.json` accepts the two-reference dry-run breadth result and keeps materialization/execution/timing/speedup out of scope. `config/scaling_gates/next_measurement_selection_after_known_template_source_closure_copy_breadth_validation_dry_run_review_gate.json` selects materialization definition next.
- Known-template source-closure copy breadth materialization defined: `config/scaling_gates/known_template_source_closure_copy_breadth_materialization_gate.json` permits exactly three new `filelist_known_template_pulp_ita_mha` source files to be written and requires the existing paged-attention copied-lineage outputs to refuse overwrite. This remains materialized source evidence only, not build/run/compare or performance evidence.
- Known-template source-closure copy breadth materialization result: `config/scaling_gates/known_template_source_closure_copy_breadth_materialization_result_gate.json` records overwrite refusal for existing paged-attention copied-lineage outputs and materializes exactly three new `filelist_known_template_pulp_ita_mha` source files. The generated template preserves complete copied source-closure metadata, `reference_template`, source-file hash, and `src/tools/build_host_probe.py`. This is not Verilator build/run/compare evidence.
- Known-template source-closure copy breadth materialization reviewed: `config/scaling_gates/known_template_source_closure_copy_breadth_materialization_review_gate.json` accepts the MHA-copy materialized source boundary and keeps execution, timing, speedup, and arbitrary dependency inference out of scope. `config/scaling_gates/next_measurement_selection_after_known_template_source_closure_copy_breadth_materialization_review_gate.json` selects materialized-template dry-run definition next.
- Known-template source-closure copy breadth materialized-template dry-run defined: `config/scaling_gates/known_template_source_closure_copy_breadth_materialized_template_dry_run_gate.json` fixes the `1x1 --dry-run` command for `config/slice_launch_templates/filelist_known_template_pulp_ita_mha.json`. The expected plan uses `src/tools/build_host_probe.py`, plans CPU reference, GPU cubin build, hybrid run, and ends in `coverage_output_equivalence`; non-dry-run execution remains a later gate.
- Known-template source-closure copy breadth materialized-template dry-run result: `config/scaling_gates/known_template_source_closure_copy_breadth_materialized_template_dry_run_result_gate.json` records exit `0` and the expected seven-stage plan for the materialized MHA-copy template. This is still command-plan evidence only, not Verilator build/run/compare, correctness, timing, speedup, or automatic dependency/allocation evidence.
- Known-template source-closure copy breadth materialized-template dry-run reviewed: `config/scaling_gates/known_template_source_closure_copy_breadth_materialized_template_dry_run_review_gate.json` accepts the dry-run plan, and `config/scaling_gates/next_measurement_selection_after_known_template_source_closure_copy_breadth_materialized_template_dry_run_review_gate.json` selects the matching non-dry-run execution-definition gate next.
- Known-template source-closure copy breadth materialized-template execution defined: `config/scaling_gates/known_template_source_closure_copy_breadth_materialized_template_execution_gate.json` selects `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/filelist_known_template_pulp_ita_mha.json --shape 1x1` as the next non-dry-run build/run/compare command. Later acceptance requires exit `0` and `coverage_output_equivalence` mismatch count `0`; timing, speedup, shape breadth, native Verilator option, arbitrary dependency inference, and automatic GPU allocation remain out of scope.
- Known-template source-closure copy breadth materialized-template execution result: `config/scaling_gates/known_template_source_closure_copy_breadth_materialized_template_execution_result_gate.json` records the non-dry-run `1x1` build/run/compare passing `coverage_output_equivalence` with mismatch count `0` over `29` words / `116` bytes. The result required copying the reference template's ITA Verilator args into the materialized copied template; raw strict final-state equality remains false with Verilator-internal-only mismatches.
- Known-template source-closure copy breadth materialized-template execution reviewed: `config/scaling_gates/known_template_source_closure_copy_breadth_materialized_template_execution_review_gate.json` accepts the scoped `1x1` execution evidence. `config/scaling_gates/next_measurement_selection_after_known_template_source_closure_copy_breadth_execution_review_gate.json` selects public results packaging refresh next before shape breadth or timing work.
- Known-template source-closure copy breadth execution public-pack refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_known_template_source_closure_copy_breadth_execution_gate.json` pins the accepted MHA-copy execution records, materialized template, coverage manifest, first benchmark gate, generator helpers, and optional compare report for public-pack dry-run verification.
- Known-template source-closure copy breadth execution public-pack refresh result: `config/scaling_gates/public_results_packaging_refresh_after_known_template_source_closure_copy_breadth_execution_result_gate.json` records the public-pack archive dry-run exit `0`, including the accepted execution records, materialized template, coverage manifest, first benchmark gate, generator helper, and optional compare report without creating an archive.
- Known-template source-closure copy breadth execution public-pack refresh reviewed: `config/scaling_gates/public_results_packaging_refresh_after_known_template_source_closure_copy_breadth_execution_review_gate.json` accepts the packaging-only dry-run result and selects the matching public benchmark pack externalization completion gate next.
- Known-template source-closure copy breadth execution public-pack externalization completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_known_template_source_closure_copy_breadth_execution_gate.json` closes the packaging boundary, and `config/scaling_gates/next_measurement_selection_after_known_template_source_closure_copy_breadth_execution_public_pack_refresh_gate.json` selects a Verilator-compatible GPU hybrid minimal bench suite next.
- Verilator-compatible GPU hybrid minimal bench suite defined and run: `config/scaling_gates/verilator_compatible_gpu_hybrid_minimal_bench_suite_gate.json` and `config/scaling_gates/verilator_compatible_gpu_hybrid_minimal_bench_suite_result_gate.json` add `python3 src/tools/run_hybrid_benchmark.py --minimal-bench-suite`, a JSON descriptor covering operator-plan preview, state-parallel high estimate, single-state repeated-step low estimate, resident decode-like dry-run, and filelist-derived MHA-copy execution evidence.
- Verilator-compatible GPU hybrid minimal bench suite checks passed: `config/scaling_gates/verilator_compatible_gpu_hybrid_minimal_bench_suite_run_result_gate.json` records `python3 src/tools/run_hybrid_benchmark.py --run-minimal-bench-suite` exiting `0`, validating the operator preview, high/low shape classes, resident dry-run, and filelist execution evidence. Existing reports provide scoped observed speedups of `157.725x` for `64x1` and `2.164x` for `1x64`.
- Verilator-compatible GPU hybrid minimal bench suite completed: `config/scaling_gates/verilator_compatible_gpu_hybrid_minimal_bench_suite_run_review_gate.json` accepts the run result, and `config/scaling_gates/verilator_compatible_gpu_hybrid_minimal_bench_suite_completion_audit.json` records that the minimal bench basis is complete for automation surface, speed tendency, filelist-derived evidence, correctness policy, docs, and tests.
- Next measured benchmark selected and defined: `config/scaling_gates/next_measured_benchmark_selection_after_verilator_compatible_gpu_hybrid_minimal_bench_suite_completion_gate.json` selects the persistent resident decode-like follow-up after multi-agent review. `config/scaling_gates/define_persistent_resident_decode_like_followup_measurement_gate.json` defines the existing `pulp_ita_mha --mode persistent-resident-state-abi` repeat-median command as the next measured gate, with direct Verilator option support, arbitrary RTL support, automatic GPU allocation, runtime/ABI changes, and production LLM serving throughput kept out of scope.
- Persistent resident decode-like follow-up measured and reviewed: `config/scaling_gates/persistent_resident_decode_like_followup_measurement_result_gate.json` records the selected command exiting `0`, all three repeat samples passing coverage-output equivalence, and mismatch count `0`. The reviewed median hybrid wall is `5.692 ms`, median GPU kernel total is `5.705824 ms`, and the next task is public-pack refresh for this scoped evidence.
- Persistent resident decode-like public-pack refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_persistent_resident_decode_like_followup_measurement_gate.json`, result gate, and review gate close the packaging dry-run for the scoped follow-up evidence. The next task is defining the public benchmark pack externalization completion boundary without adding measurement, runtime/ABI, direct Verilator option, arbitrary RTL, or automatic GPU allocation claims.
- Persistent resident decode-like public-pack externalization completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_persistent_resident_decode_like_followup_measurement_gate.json` closes the externalization boundary for the scoped follow-up evidence. The next task is selecting the next measured benchmark after this public-pack refresh.
- Next measured benchmark selected after persistent resident decode-like refresh: `config/scaling_gates/next_measurement_selection_after_persistent_resident_decode_like_followup_public_pack_refresh_gate.json` selects `define_filelist_shape_breadth_timing_measurement_gate`. The next gate should define filelist-derived timing and shape breadth without claiming native Verilator option support, arbitrary RTL dependency inference, automatic optimal GPU allocation, runtime/ABI change, or production LLM-serving throughput.
- Filelist shape-breadth timing measurement defined: `config/scaling_gates/define_filelist_shape_breadth_timing_measurement_gate.json` fixes four commands: `filelist_paged_attention_kv_score` at `32x1` and `1x32`, plus `filelist_known_template_pulp_ita_mha` at `32x1` and `1x32`. The next task is running those commands and recording scoped single-run timing plus `coverage_output_equivalence`.
- Filelist shape-breadth timing measurement result recorded: `config/scaling_gates/filelist_shape_breadth_timing_measurement_result_gate.json` records all four commands exiting `0` with `coverage_output_equivalence` mismatch count `0`. Observed wall speedup is about `87x` for both `32x1` state-parallel filelist-derived templates and about `2.35x` to `3.33x` for `1x32` single-state repeated-step shapes; this remains single-run scoped evidence, not repeat-median or broad speedup evidence.
- Filelist shape-breadth timing measurement reviewed: `config/scaling_gates/filelist_shape_breadth_timing_measurement_review_gate.json` accepts the scoped four-command result and keeps raw full-state equality diagnostic only. The next task is defining a public results packaging refresh for this evidence without adding new measurement, native Verilator option, arbitrary RTL dependency inference, automatic GPU allocation, or production serving claims.
- Filelist shape-breadth public-pack refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_timing_measurement_gate.json` defines the archive dry-run boundary for the accepted four-command result. The next task is running the public-pack dry-run and confirming that the definition, result, review, and refresh definition gates are included without broadening timing, runtime, native-option, arbitrary-RTL, or automatic-allocation claims.
- Filelist shape-breadth public-pack refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_timing_measurement_result_gate.json` records `python3 src/tools/run_results_reproduction.py --public-pack-archive --dry-run` exiting `0`, and `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_timing_measurement_review_gate.json` accepts the packaging-only result. The next task is defining the public benchmark pack externalization completion boundary for this scoped evidence.
- Filelist shape-breadth public-pack externalization completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_timing_measurement_gate.json` closes the public benchmark pack boundary for the accepted four-command result. The next task is selecting the next measured benchmark after this public-pack refresh without adding measurement, runtime/ABI, native Verilator option, arbitrary RTL dependency inference, automatic GPU allocation, or repeat-median claims.
- Next measured benchmark selected after filelist shape-breadth public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_timing_measurement_public_pack_refresh_gate.json` selects `define_filelist_shape_breadth_repeat_median_timing_gate`. The reason is that the accepted filelist timing evidence is still single-run; the next gate should define repeat-median timing for the same target/shape pairs before deriving automatic GPU allocation policy.
- Filelist shape-breadth repeat-median timing boundary defined: `config/scaling_gates/define_filelist_shape_breadth_repeat_median_timing_gate.json` fixes the same four filelist-derived target/shape pairs with repeat count `3`, but records that no previous public repeat-median CLI covered this exact filelist set.
- Filelist shape-breadth repeat-median workflow added: `config/scaling_gates/filelist_shape_breadth_repeat_median_workflow_gate.json` adds `python3 src/tools/run_results_reproduction.py --filelist-shape-breadth-repeat-median 3 --dry-run`, expanding the four target/shape pairs into three samples each without writing generated evidence in dry-run. The workflow gate itself adds no timing evidence; the later measurement gate records the scoped repeat-median result.
- Filelist shape-breadth repeat-median measured: `config/scaling_gates/filelist_shape_breadth_repeat_median_measurement_gate.json` records `python3 src/tools/run_results_reproduction.py --filelist-shape-breadth-repeat-median 3` exiting `0`. All 12 samples pass `coverage_output_equivalence` with mismatch count `0`; median CPU-to-hybrid wall ratios are about `68.83x` and `76.08x` for `32x1`, versus about `2.68x` and `6.71x` for `1x32`. This is scoped repeat-count-3 evidence only, not broad speedup, native Verilator option support, arbitrary RTL dependency inference, automatic GPU allocation, or production serving evidence.
- Filelist shape-breadth repeat-median reviewed: `config/scaling_gates/filelist_shape_breadth_repeat_median_review_gate.json` accepts the scoped repeat-count-3 evidence for public-pack refresh. The weakest point remains sample variability, especially the `filelist_known_template_pulp_ita_mha 1x32` CPU samples, so the accepted claim is a scoped trend only: `32x1` state-parallel remains more favorable than `1x32` repeated-step for these exact filelist-derived templates.
- Filelist shape-breadth repeat-median public-pack refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_repeat_median_gate.json` closes the reviewed repeat-count-3 evidence into the public-pack boundary. The next task is running `python3 src/tools/run_results_reproduction.py --public-pack-archive --dry-run` and confirming the boundary, workflow, measurement, review, and refresh definition gates plus the generated median summaries are included without treating reports as source of truth.
- Filelist shape-breadth repeat-median public-pack refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_repeat_median_result_gate.json` records the public-pack dry-run exiting `0`, and `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_repeat_median_review_gate.json` accepts the packaging-only result. This led into the public benchmark pack externalization completion boundary for the scoped repeat-count-3 evidence.
- Filelist shape-breadth repeat-median public-pack externalization completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_repeat_median_gate.json` closes the publication boundary for the scoped repeat-count-3 evidence. The next task is selecting the next measured benchmark after this public-pack refresh; likely candidates include scoped automatic GPU allocation policy, broader filelist shape sweep, native-option boundary, dependency inference policy, and resident execution optimization.
- Next measured workstream selected after filelist shape-breadth repeat-median public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_repeat_median_public_pack_refresh_gate.json` selects `define_filelist_shape_breadth_gpu_allocation_policy_gate`. The reason is that automatic GPU allocation policy was previously deferred until repeat-median-backed filelist timing existed; the selected next step is a scoped policy boundary, not automatic optimal allocation for arbitrary RTL or native Verilator option support.
- Filelist shape-breadth GPU allocation policy boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_gate.json` limits the first policy to the two reviewed filelist-derived templates, `32x1`/`1x32`, and repeat-count `3` evidence. It defines output fields such as recommended `nstates`, `steps`, confidence, reason, and fallback/refusal behavior; this remains scoped policy work, not runtime/ABI changes or arbitrary RTL optimal allocation.
- Filelist shape-breadth GPU allocation policy dry-run accepted: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_dry_run_result_gate.json` records `python3 src/tools/run_results_reproduction.py --filelist-shape-breadth-gpu-allocation-policy --dry-run` exiting `0`, and `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_dry_run_review_gate.json` accepts the scoped JSON-only policy surface. The next task is defining policy execution/application for the reviewed filelist targets, still not arbitrary RTL optimal allocation, native Verilator support, runtime/ABI change, or new timing evidence.
- Filelist shape-breadth GPU allocation policy execution boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_execution_gate.json` defines execution/application as applying the accepted `32x1` policy to existing tracked template command plans first, using dry-run validation for the two reviewed targets. It does not select non-dry-run execution, mutate templates, change runtime/ABI behavior, or claim arbitrary RTL optimal allocation.
- Filelist shape-breadth GPU allocation policy execution dry-run accepted: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_execution_dry_run_result_gate.json` records both reviewed `32x1 --dry-run` tracked-template command plans exiting `0`, and `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_execution_dry_run_review_gate.json` accepts command-plan evidence only. The next task is defining any non-dry-run execution boundary and exact command list.
- Filelist shape-breadth GPU allocation policy non-dry-run execution boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_non_dry_run_execution_gate.json` selects both reviewed `32x1` tracked-template commands for build/run/compare. Later acceptance requires command exit `0` and `coverage_output_equivalence` mismatch count `0`; this gate itself is definition-only and does not add timing, native Verilator, arbitrary RTL, or automatic optimal allocation claims.
- Filelist shape-breadth GPU allocation policy non-dry-run execution accepted: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_non_dry_run_execution_result_gate.json` records both reviewed `32x1` build/run/compare commands exiting `0`, writing the expected compare reports, and passing `coverage_output_equivalence` with mismatch count `0` over `29` words / `116` bytes per state. `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_non_dry_run_execution_review_gate.json` accepts only that scoped two-command execution and selects public results packaging refresh next; raw full-state equality remains false and timing, native Verilator, arbitrary RTL, runtime/ABI, and automatic optimal allocation claims remain out of scope.
- Filelist shape-breadth GPU allocation policy non-dry-run public-pack refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_non_dry_run_execution_gate.json` defines the packaging-only refresh, `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_non_dry_run_execution_result_gate.json` records the public-pack archive dry-run exiting `0`, and `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_non_dry_run_execution_review_gate.json` accepts the refresh. The next task is defining public benchmark-pack externalization completion for this scoped result; this adds no new execution, timing, native Verilator, arbitrary RTL, runtime/ABI, or automatic optimal allocation claim.
- Filelist shape-breadth GPU allocation policy non-dry-run public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_non_dry_run_execution_gate.json` closes the public benchmark-pack boundary for the scoped two-command `32x1` execution result. The next task is selecting the next measurement after this public-pack refresh; no new measurement, execution, timing/speedup, native Verilator, arbitrary RTL/dependency inference, runtime/ABI, or automatic optimal allocation claim is added.
- Next measured workstream selected after filelist GPU allocation policy non-dry-run public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_non_dry_run_execution_public_pack_refresh_gate.json` selects `define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_gate`. The reason is that the current non-dry-run evidence is still only two reviewed filelist-derived targets at `32x1`; broader explicit shape evidence should come before native Verilator option work, arbitrary RTL dependency inference, runtime/ABI work, or automatic optimal allocation claims.
- Filelist shape-breadth GPU allocation policy broader shape sweep defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_gate.json` selects four dry-run command plans for the same two reviewed filelist-derived targets at `64x1` and `1x64`. It keeps `32x1` as the accepted state-parallel baseline and `1x32` as the existing repeated-step control; GEM comparison, native Verilator option work, runtime/ABI work, arbitrary RTL, and automatic optimal allocation remain deferred.
- Filelist shape-breadth GPU allocation policy broader shape sweep dry-run accepted: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_dry_run_result_gate.json` records all four `64x1` / `1x64` command plans exiting `0`, and `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_dry_run_review_gate.json` accepts only command-plan evidence. This led to defining exact non-dry-run execution; dry-run remains not correctness, timing/speedup, GEM comparison, native Verilator, arbitrary RTL, runtime/ABI, or automatic optimal allocation evidence.
- Filelist shape-breadth GPU allocation policy broader shape sweep non-dry-run boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_non_dry_run_execution_gate.json` selects the four accepted `64x1` / `1x64` build/run/compare commands for the same two reviewed filelist-derived targets. The next task is running those commands and requiring exit `0` plus `coverage_output_equivalence` mismatch count `0`; this definition itself is not execution, timing/speedup, GEM comparison, native Verilator, arbitrary RTL, runtime/ABI, or automatic optimal allocation evidence.
- Filelist shape-breadth GPU allocation policy broader shape sweep non-dry-run accepted: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_non_dry_run_execution_result_gate.json` records all four `64x1` / `1x64` build/run/compare commands exiting `0`, writing the expected compare reports, and passing `coverage_output_equivalence` with mismatch count `0`. `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_non_dry_run_execution_review_gate.json` accepts that scoped evidence and selects public results packaging refresh next; raw full-state equality remains false, and timing/speedup, repeat-median, GEM comparison, native Verilator, arbitrary RTL, runtime/ABI, production-serving, and automatic optimal allocation remain non-claims.
- Filelist shape-breadth GPU allocation policy broader shape sweep public-pack refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_non_dry_run_execution_gate.json` defines the packaging-only refresh, `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_non_dry_run_execution_result_gate.json` records the public-pack archive dry-run exiting `0`, and `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_non_dry_run_execution_review_gate.json` accepts the refresh. The next task is defining public benchmark-pack externalization completion for this scoped four-command result; this adds no new execution, timing/speedup, repeat-median, GEM comparison, native Verilator, arbitrary RTL, runtime/ABI, production-serving, or automatic optimal allocation claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_non_dry_run_execution_gate.json` closes the publication boundary for the scoped four-command `64x1` / `1x64` result. The next task is selecting the next measurement after this public-pack refresh; no new measurement, execution, timing/speedup, repeat-median, GEM comparison, native Verilator, arbitrary RTL, runtime/ABI, production-serving, or automatic optimal allocation claim is added.
- Next measured workstream selected after broader shape-sweep public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_non_dry_run_execution_public_pack_refresh_gate.json` selects `define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_gate`. The reason is that the accepted `64x1` / `1x64` evidence is correctness-only; timing for the same four target/shape pairs should come before repeat-median, resident optimization, native Verilator option work, dependency inference, GEM comparison, or policy-broadening claims.
- Filelist shape-breadth GPU allocation policy broader shape sweep timing boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_gate.json` fixes the same two reviewed filelist-derived targets at `64x1` and `1x64`, using the four existing `run_hybrid_template.py` commands and single-run timing fields. The next task is running those four commands and recording timing plus `coverage_output_equivalence`; this definition adds no timing evidence, repeat-median evidence, native Verilator support, arbitrary RTL/dependency inference, runtime/ABI change, GEM comparison, production-serving, or automatic optimal allocation claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep timing measured: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_result_gate.json` records the same four commands exiting `0`, passing `coverage_output_equivalence` with mismatch count `0`, and producing single-run CPU/hybrid timing. Observed wall ratios are `127.407767x` and `8.297071x` for `filelist_paged_attention_kv_score` at `64x1` / `1x64`, and `214.308914x` and `1.945839x` for `filelist_known_template_pulp_ita_mha`; this is not repeat-median, broad speedup, GEM comparison, native Verilator, arbitrary RTL/dependency inference, runtime/ABI, production-serving, automatic optimal allocation, or raw full-state equality evidence.
- Filelist shape-breadth GPU allocation policy broader shape sweep timing reviewed: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_review_gate.json` accepts the scoped single-run timing result and selects public results packaging refresh next. It allows only the scoped single-run timing tendency for the two reviewed filelist-derived targets at `64x1` / `1x64`; repeat-median, broad speedup, GEM comparison, native Verilator, arbitrary RTL/dependency inference, runtime/ABI, production-serving, automatic optimal allocation, and raw full-state equality remain non-claims.
- Filelist shape-breadth GPU allocation policy broader shape sweep timing public-pack refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_result_gate.json` records the public-pack archive dry-run exiting `0` and including the timing definition/result/review, source-of-truth docs, compact selection pointers, and twelve generated timing/compare reports as evidence-only paths. `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_review_gate.json` accepts the refresh and selects public benchmark-pack externalization completion next; no new measurement, execution, repeat-median, broad speedup, GEM, native Verilator, arbitrary RTL/dependency inference, runtime/ABI, production-serving, automatic allocation, or raw full-state equality claim is added.
- Filelist shape-breadth GPU allocation policy broader shape sweep timing public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_gate.json` closes the publication boundary for the scoped single-run timing refresh. It carries only the reviewed `coverage_output_equivalence` mismatch count `0` and single-run timing ranges for two filelist-derived targets at `64x1` / `1x64`, then selects next-measurement selection; repeat-median, broad speedup, GEM comparison, native Verilator, arbitrary RTL/dependency inference, runtime/ABI, production-serving, automatic allocation, and raw full-state equality remain non-claims.
- Next measured workstream selected after broader shape-sweep timing public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_timing_measurement_public_pack_refresh_gate.json` selects `define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_timing_gate`. This gate is selection-only: it adds no new measurement, execution, repeat-median evidence, broad speedup, GEM comparison, native Verilator option support, arbitrary RTL/dependency inference, runtime/ABI change, production-serving, automatic optimal allocation, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median workflow added: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_workflow_gate.json` adds the public `--filelist-broader-shape-repeat-median` workflow for the same `64x1` / `1x64` target/shape pairs. This workflow gate adds no repeat-median result, policy broadening, native Verilator option, dependency inference, runtime/ABI, GEM, production-serving, or automatic allocation claim by itself; the later measurement gate records the scoped result and selects review next.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median measured: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_measurement_gate.json` records the scoped repeat-count-3 result and selects review next. State-parallel `64x1` remains materially stronger than single-state repeated-step `1x64` for these exact filelist-derived templates; broad speedup, native Verilator option, arbitrary RTL/dependency inference, runtime/ABI, GEM, production-serving, automatic allocation, and raw full-state equality remain non-claims.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median reviewed: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_review_gate.json` accepts the scoped result for public-pack packaging only and advances the current task to `define_public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median public refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_gate.json` advances the current task to `run_public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_review_gate.json` advances the current task to `define_public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep repeat-median public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_gate.json` advances the current task to `select_next_measurement_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_public_pack_refresh`.
- Next measured workstream selected after broader shape-sweep repeat-median public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_repeat_median_public_pack_refresh_gate.json` advances the current task to `define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_gate.json` advances the current task to `add_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_workflow`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy workflow added: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_workflow_gate.json` advanced the task to the now-accepted dry-run review.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy dry-run reviewed and public refresh defined: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_dry_run_review_gate.json` accepts the JSON-only dry-run payload, and `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_gate.json` defines the packaging-only refresh. This adds no execution, measurement, runtime/ABI, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, GEM, production-serving, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_result_gate.json` records the public-pack archive dry-run exiting `0`, and `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_review_gate.json` accepts the packaging-only refresh. This adds no execution, measurement, runtime/ABI, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, GEM, production-serving, or raw full-state equality claim.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy public-pack externalization complete: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_gate.json` closes the publication boundary for the scoped dry-run policy evidence. This adds no execution, measurement, runtime/ABI, native Verilator option, arbitrary RTL/dependency inference, automatic optimal allocation, GEM, production-serving, or raw full-state equality claim.
- Next measured workstream selected after broader shape-sweep policy public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_public_pack_refresh_gate.json` advances the current task to `define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy non-dry-run execution accepted: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_review_gate.json` advances the current task to `define_public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy non-dry-run public refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate.json` advances the current task to `run_public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy non-dry-run public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_review_gate.json` advances the current task to `define_public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy non-dry-run public-pack externalization completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_gate.json` advances the current task to `select_next_measurement_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_public_pack_refresh`.
- Next measured workstream selected after broader shape-sweep policy non-dry-run public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_non_dry_run_execution_public_pack_refresh_gate.json` advances the current task to `define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_timing_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median timing boundary defined: `config/scaling_gates/define_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_timing_gate.json` advances the current task to `add_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_workflow`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median workflow added: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_workflow_gate.json` advances the current task to `run_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_dry_run_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median dry-run accepted: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_dry_run_review_gate.json` advances the current task to `run_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_timing_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median reviewed: `config/scaling_gates/filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_review_gate.json` advances the current task to `define_public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_review_gate.json` advances the current task to `define_public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_gate`.
- Filelist shape-breadth GPU allocation policy broader shape sweep policy repeat-median public-pack externalization completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_gate.json` advances the current task to `select_next_measurement_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_public_pack_refresh`.
- Next measured workstream selected after policy-selected repeat-median public-pack refresh: `config/scaling_gates/next_measurement_selection_after_filelist_shape_breadth_gpu_allocation_policy_broader_shape_sweep_policy_repeat_median_public_pack_refresh_gate.json` advanced the task to `define_verilator_native_option_prototype_boundary_gate`.
- Verilator native option prototype boundary defined: `config/scaling_gates/define_verilator_native_option_prototype_boundary_gate.json` advances the current task to `run_verilator_native_option_prototype_boundary_dry_run_gate`.
- Verilator native option prototype boundary dry-run accepted: `config/scaling_gates/verilator_native_option_prototype_boundary_dry_run_review_gate.json` advances the current task to `define_verilator_native_option_prototype_filelist_target_resolution_gate`.
- Verilator native option prototype filelist registry accepted: `config/scaling_gates/verilator_native_option_prototype_filelist_registry_review_gate.json` advances the current task to `define_public_results_packaging_refresh_after_verilator_native_option_prototype_filelist_registry_gate`.
- Verilator native option prototype filelist registry public refresh defined: `config/scaling_gates/public_results_packaging_refresh_after_verilator_native_option_prototype_filelist_registry_gate.json` advances the current task to `run_public_results_packaging_refresh_after_verilator_native_option_prototype_filelist_registry_gate`.
- Verilator native option prototype filelist registry public refresh accepted: `config/scaling_gates/public_results_packaging_refresh_after_verilator_native_option_prototype_filelist_registry_review_gate.json` advances the current task to `define_public_benchmark_pack_externalization_completion_after_verilator_native_option_prototype_filelist_registry_gate`.
- Verilator native option prototype filelist registry public-pack externalization completed: `config/scaling_gates/public_benchmark_pack_externalization_completion_after_verilator_native_option_prototype_filelist_registry_gate.json` advances the current task to `select_next_measurement_after_verilator_native_option_prototype_filelist_registry_public_pack_refresh`.
- Next workstream selected after Verilator native option prototype filelist registry public-pack refresh: `config/scaling_gates/next_measurement_selection_after_verilator_native_option_prototype_filelist_registry_public_pack_refresh_gate.json` advances the current task to `define_commit_split_and_public_pack_cleanup_gate`.
- Commit split and public-pack cleanup boundary defined: `config/scaling_gates/define_commit_split_and_public_pack_cleanup_gate.json` advances the current task to `review_commit_split_and_public_pack_cleanup_gate`.
- Commit split and public-pack cleanup review accepted: `config/scaling_gates/review_commit_split_and_public_pack_cleanup_gate.json` advances the current task to `resolve_commit_split_line_guard_risks_gate`.
- Commit split line guard risks resolved: `config/scaling_gates/resolve_commit_split_line_guard_risks_gate.json` advances the current task to `execute_commit_split_index_rewrite_gate`.
- Commit split index rewrite completed: `config/scaling_gates/execute_commit_split_index_rewrite_gate.json` advances the current task to `define_verilator_native_option_parser_boundary_gate`.
- Verilator native option parser boundary defined: `config/scaling_gates/define_verilator_native_option_parser_boundary_gate.json` advances the current task to `review_verilator_native_option_parser_boundary_gate`.
- Verilator native option parser boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_boundary_gate.json` advances the current task to `run_verilator_native_option_parser_boundary_dry_run_gate`.
- Verilator native option parser boundary dry-run passed: `config/scaling_gates/verilator_native_option_parser_boundary_dry_run_result_gate.json` advances the current task to `review_verilator_native_option_parser_boundary_dry_run_gate`.
- Verilator native option parser boundary dry-run review accepted: `config/scaling_gates/review_verilator_native_option_parser_boundary_dry_run_gate.json` advances the current task to `define_verilator_native_option_parser_stub_boundary_gate`.
- Verilator native option parser stub boundary defined: `config/scaling_gates/define_verilator_native_option_parser_stub_boundary_gate.json` advances the current task to `review_verilator_native_option_parser_stub_boundary_gate`.
- Verilator native option parser stub boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_stub_boundary_gate.json` advances the current task to `define_verilator_native_option_parser_stub_fixture_gate`.
- Verilator native option parser stub fixture defined: `config/scaling_gates/define_verilator_native_option_parser_stub_fixture_gate.json` advances the current task to `review_verilator_native_option_parser_stub_fixture_gate`.
- Verilator native option parser stub fixture review accepted: `config/scaling_gates/review_verilator_native_option_parser_stub_fixture_gate.json` advances the current task to `implement_verilator_native_option_parser_stub_fixture_gate`.
- Verilator native option parser stub fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_stub_fixture_gate.json` advances the current task to `review_verilator_native_option_parser_stub_fixture_implementation_gate`.
- Verilator native option parser stub fixture implementation review accepted: `config/scaling_gates/review_verilator_native_option_parser_stub_fixture_implementation_gate.json` advances the current task to `define_verilator_native_option_parser_source_patch_boundary_gate`.
- Verilator native option parser source-patch boundary defined: `config/scaling_gates/define_verilator_native_option_parser_source_patch_boundary_gate.json` advances the current task to `review_verilator_native_option_parser_source_patch_boundary_gate`.
- Verilator native option parser source-patch boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_source_patch_boundary_gate.json` advances the current task to `define_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate`.
- Verilator native option parser overlay patch descriptor/apply-check boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate.json` advances the current task to `review_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate`.
- Verilator native option parser overlay patch descriptor/apply-check review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate.json` advances the current task to `implement_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate`.
- Verilator native option parser overlay patch descriptor/apply-check payload implemented: `config/scaling_gates/implement_verilator_native_option_parser_overlay_patch_descriptor_apply_check_gate.json` advances the current task to `review_verilator_native_option_parser_overlay_patch_descriptor_apply_check_implementation_gate`.
- Verilator native option parser overlay patch descriptor/apply-check implementation review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_descriptor_apply_check_implementation_gate.json` advances the current task to `define_verilator_native_option_parser_overlay_patch_build_only_validation_gate`.
- Verilator native option parser overlay patch build-only validation boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_build_only_validation_gate.json` advances the current task to `review_verilator_native_option_parser_overlay_patch_build_only_validation_gate`.
- Verilator native option parser overlay patch build-only validation boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_build_only_validation_gate.json` advances the current task to `run_verilator_native_option_parser_overlay_patch_build_only_validation_gate`.
- Verilator native option parser overlay patch build-only validation run failed at compile: `config/scaling_gates/run_verilator_native_option_parser_overlay_patch_build_only_validation_gate.json` records descriptor/apply-check success, checkout-local `VERILATOR_INSTALL`, `verilator_bin` reached, and `make` exit code `2` because `V3Options.h` additions landed after `#endif  // guard`. The current task is `define_verilator_native_option_parser_overlay_patch_compile_fix_gate`; this is not parser behavior, execution, timing, or arbitrary RTL/filelist evidence.
- Verilator native option parser overlay patch compile-fix boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_compile_fix_gate.json` advances the current task to `review_verilator_native_option_parser_overlay_patch_compile_fix_gate`.
- Verilator native option parser overlay patch compile-fix boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_compile_fix_gate.json` advances the current task to `implement_verilator_native_option_parser_overlay_patch_compile_fix_gate`.
- Verilator native option parser overlay patch compile-fix implemented: `config/scaling_gates/implement_verilator_native_option_parser_overlay_patch_compile_fix_gate.json` advances the current task to `run_verilator_native_option_parser_overlay_patch_compile_fix_build_only_validation_gate`.
- Verilator native option parser overlay patch compile-fix build-only validation passed: `config/scaling_gates/run_verilator_native_option_parser_overlay_patch_compile_fix_build_only_validation_gate.json` advances the current task to `review_verilator_native_option_parser_overlay_patch_compile_fix_build_only_validation_gate`.
- Verilator native option parser overlay patch compile-fix build-only validation review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_compile_fix_build_only_validation_gate.json` advances the current task to `define_verilator_native_option_parser_overlay_patch_parser_behavior_gate`.
- Verilator native option parser overlay patch parser-behavior boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_parser_behavior_gate.json` advances the current task to `review_verilator_native_option_parser_overlay_patch_parser_behavior_gate`.
- Verilator native option parser overlay patch parser-behavior boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_parser_behavior_gate.json` advances the current task to `run_verilator_native_option_parser_overlay_patch_parser_behavior_gate`.
- Verilator native option parser overlay patch parser-behavior smoke passed: `config/scaling_gates/run_verilator_native_option_parser_overlay_patch_parser_behavior_gate.json` advances the current task to `review_verilator_native_option_parser_overlay_patch_parser_behavior_run_gate`.
- Verilator native option parser overlay patch parser-behavior smoke review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_parser_behavior_run_gate.json` advances the current task to `define_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate`.
- Verilator native option parser overlay patch parser-integer hardening boundary defined: `config/scaling_gates/define_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate.json` advances the current task to `review_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate`.
- Verilator native option parser overlay patch parser-integer hardening boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate.json` advances the current task to `implement_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate`.
- Verilator native option parser overlay patch parser-integer hardening implementation accepted: `config/scaling_gates/implement_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate.json` advances the current task to `run_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate`.
- Verilator native option parser overlay patch parser-integer hardening smoke passed: `config/scaling_gates/run_verilator_native_option_parser_overlay_patch_parser_integer_hardening_gate.json` advances the current task to `review_verilator_native_option_parser_overlay_patch_parser_integer_hardening_run_gate`.
- Verilator native option parser overlay patch parser-integer hardening smoke review accepted: `config/scaling_gates/review_verilator_native_option_parser_overlay_patch_parser_integer_hardening_run_gate.json` advances the current task to `define_verilator_native_option_parser_sidecar_handoff_boundary_gate`.
- Verilator native option parser-to-sidecar handoff boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_handoff_boundary_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_handoff_boundary_gate`.
- Verilator native option parser-to-sidecar handoff boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_sidecar_handoff_boundary_gate.json` advances the current task to `implement_verilator_native_option_parser_sidecar_handoff_fixture_gate`.
- Verilator native option parser-to-sidecar handoff fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_handoff_fixture_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_handoff_fixture_implementation_gate`.
- Verilator native option parser-to-sidecar handoff fixture review accepted: `config/scaling_gates/review_verilator_native_option_parser_sidecar_handoff_fixture_implementation_gate.json` advances the current task to `define_verilator_native_option_parser_sidecar_plan_resolution_boundary_gate`.
- Verilator native option parser-to-sidecar plan-resolution boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_boundary_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_boundary_gate`.
- Verilator native option parser-to-sidecar plan-resolution boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_boundary_gate.json` advances the current task to `implement_verilator_native_option_parser_sidecar_plan_resolution_fixture_gate`.
- Verilator native option parser-to-sidecar plan-resolution fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_plan_resolution_fixture_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_fixture_implementation_gate`.
- Verilator native option parser-to-sidecar plan-resolution fixture review accepted: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_fixture_implementation_gate.json` advances the current task to `define_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_gate`.
- Verilator native option parser-to-sidecar plan-resolution status hardening defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_gate`.
- Verilator native option parser-to-sidecar plan-resolution status hardening review accepted: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_gate.json` advances the current task to `implement_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_gate`.
- Verilator native option parser-to-sidecar plan-resolution status hardening implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_implementation_gate`.
- Verilator native option parser-to-sidecar plan-resolution status hardening implementation review accepted: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_status_hardening_implementation_gate.json` advances the current task to `define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_boundary_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_boundary_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_boundary_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_boundary_gate.json` advances the current task to `implement_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_fixture_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_fixture_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_fixture_implementation_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract fixture implementation review accepted: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_fixture_implementation_gate.json` advances the current task to `define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_boundary_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_boundary_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_boundary_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan boundary review accepted: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_boundary_gate.json` advances the current task to `implement_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_implementation_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture implementation reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_implementation_gate.json` advances the current task to `define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture hardening boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture hardening boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_gate.json` advances the current task to `implement_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture hardening implemented: `config/scaling_gates/implement_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_implementation_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan fixture hardening implementation reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_fixture_hardening_implementation_gate.json` advances the current task to `define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_execution_boundary_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan execution boundary defined: `config/scaling_gates/define_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_execution_boundary_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_execution_boundary_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan execution boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_execution_boundary_gate.json` advances the current task to `run_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_sidecar_execution_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan sidecar execution run passed: `config/scaling_gates/run_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_sidecar_execution_gate.json` advances the current task to `review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_sidecar_execution_run_gate`.
- Verilator native option parser-to-sidecar plan-resolution handoff-contract-to-operator-plan sidecar execution run reviewed: `config/scaling_gates/review_verilator_native_option_parser_sidecar_plan_resolution_handoff_contract_operator_plan_sidecar_execution_run_gate.json` advances the current task to `define_verilator_native_option_parser_direct_command_path_boundary_gate`.
- Verilator native option parser direct command-path boundary defined: `config/scaling_gates/define_verilator_native_option_parser_direct_command_path_boundary_gate.json` advances the current task to `review_verilator_native_option_parser_direct_command_path_boundary_gate`.
- Verilator native option parser direct command-path boundary reviewed: `config/scaling_gates/review_verilator_native_option_parser_direct_command_path_boundary_gate.json` advances the current task to `define_verilator_native_option_parser_direct_command_path_fixture_gate`.
- Verilator native option parser direct command-path fixture contract defined: `config/scaling_gates/define_verilator_native_option_parser_direct_command_path_fixture_gate.json` advances the current task to `review_verilator_native_option_parser_direct_command_path_fixture_gate`.
- Verilator native option parser direct command-path fixture contract reviewed: `config/scaling_gates/review_verilator_native_option_parser_direct_command_path_fixture_gate.json` advances the current task to `implement_verilator_native_option_parser_direct_command_path_fixture_gate`.
- Verilator native option parser direct command-path fixture implemented: `config/scaling_gates/implement_verilator_native_option_parser_direct_command_path_fixture_gate.json` advances the current task to `review_verilator_native_option_parser_direct_command_path_fixture_implementation_gate`.
- Verilator native option parser direct command-path fixture implementation reviewed: `config/scaling_gates/review_verilator_native_option_parser_direct_command_path_fixture_implementation_gate.json` advances the current task to `define_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate`.
- Verilator native option parser direct command-path fixture payload validation hardening defined: `config/scaling_gates/define_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate.json` advances the current task to `review_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate`.
- Verilator native option parser direct command-path fixture payload validation hardening reviewed: `config/scaling_gates/review_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate.json` advances the current task to `implement_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate`.
- Verilator native option parser direct command-path fixture payload validation hardening implemented: `config/scaling_gates/implement_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_gate.json` advances the current task to `review_verilator_native_option_parser_direct_command_path_fixture_payload_validation_hardening_implementation_gate`.
- config-generation validation breadth execution and subsequent candidate measurement/publication gates are summarized by `config/selection.json`, `docs/status.md`, and the tracked public-pack manifest instead of being listed here one gate file at a time
- local candidate gate files under `records/scaling_gates/` are not public-pack source of truth until they are intentionally tracked and included by `python3 src/tools/run_results_reproduction.py --public-pack-archive --dry-run`
- previous paged-attention/KV-cache dry-run commands: `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/pulp_paged_kv_cache_large.json --shape 256x1 --dry-run`, `python3 src/tools/run_hybrid_template.py config/slice_launch_templates/pulp_paged_kv_cache_large.json --shape 1x64 --dry-run`, `python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --shape 64x1 --dry-run`, and `python3 src/tools/run_hybrid_benchmark.py paged_attention_kv_score --shape 1x64 --dry-run`
- do not import unreviewed candidate overlays or change runtime/ABI behavior for this selection boundary
- keep the long-term goal: make hybrid execution close to Verilator usage while preserving reproducible correctness and speed evidence for LLM-serving-like RTL workloads
- keep config current-state-only and historical evidence in tracked gate records; regenerate reports/artifacts only when needed
- keep public CLIs thin and tested
