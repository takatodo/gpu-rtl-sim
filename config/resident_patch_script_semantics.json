{
  "schema_version": 1,
  "name": "resident_patch_script_semantics",
  "status": "packaged_bounded_multi_seed_gpu_wins",
  "purpose": "Define and implement resident-mode patch/script semantics before allowing changing-input workloads to claim throughput.",
  "weakest_point": "The resident changing-input GPU and matching CPU gates pass with a bounded 512x32 GPU win, but this does not imply broad target-breadth or full RTL application throughput.",
  "accepted_semantics": {
    "state_residency": "The full state array remains device-resident across all resident eval steps.",
    "initial_state_upload": "The optional init-state upload is performed once before the resident loop begins.",
    "patch_schedule_upload": "Per-step patch changes are uploaded once as a compact schedule before the resident loop begins.",
    "per_step_host_copy": "No cuMemcpyHtoD patch copy may occur inside the resident step loop.",
    "final_state_download": "Final DtoH occurs only when --dump-state is requested."
  },
  "patch_schedule_abi": {
    "host_input_text_format": "Reuse the existing --patch-script text grammar as the source format.",
    "device_schedule_encoding": "A compact array of patch records plus per-step index metadata is prepared before resident execution.",
    "patch_record_fields": [
      "logical_step",
      "global_byte_offset",
      "byte_value"
    ],
    "empty_step_encoding": "A step with '-' has zero records and still advances one resident eval step.",
    "repeat_encoding": "@repeat-seq blocks are expanded or indexed before resident execution; runtime semantics must be equivalent to the non-resident parser.",
    "offset_scope": "global_byte_offset is relative to the full device storage buffer, matching the current --patch syntax."
  },
  "implementation_policy": {
    "phase_1": "Keep rejecting direct --patch with --resident-steps because it is still a host argv per-step patch interface.",
    "phase_2": "Allow --resident-steps with --patch-script by uploading the schedule once before launch and applying records with vl_apply_patch_schedule_gpu.",
    "phase_3": "Consider direct --patch with --resident-steps only as a one-step schedule shorthand.",
    "forbidden": [
      "per-step host-device patch copies in resident mode",
      "changing non-resident --patch-script semantics while adding resident support",
      "claiming resident changing-input throughput before a matching CPU baseline exists"
    ]
  },
  "next_semantics_axis": {
    "name": "application_like_patch_schedule_semantics",
    "purpose": "Move beyond arbitrary root byte patches toward input, ROM, or program-delta changes that can be explained as design-level stimuli.",
    "candidate_semantics": [
      "input_stream_delta",
      "rom_or_memory_init_delta",
      "program_image_delta"
    ],
    "non_goals": [
      "do not treat arbitrary root byte offsets as application semantics",
      "do not claim ISA/program correctness from byte-patch throughput alone",
      "do not reintroduce per-step host-device copies"
    ]
  },
  "next_action": "define_application_like_patch_schedule_semantics"
}
