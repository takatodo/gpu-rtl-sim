{
  "schema_version": 1,
  "gate": "veer_el2_larger_resident_workload_scaling",
  "target": "veer_el2",
  "source_launch_template": "config/slice_launch_templates/veer_el2.json",
  "purpose": "Increase the VeeR-EL2 resident runtime workload after the first resident gate passed but remained CPU-favorable.",
  "preconditions": [
    "config/scaling_gates/veer_el2_resident_workload.json passes",
    "reports/veer_el2_cpu_exact_loop_resident_workload.json shows the first resident gate is CPU-favorable",
    "artifacts/veer_el2_obj_dir/vl_batch_gpu.cubin exists",
    "artifacts/ and reports/ are ignored generated outputs"
  ],
  "runs": [
    {
      "name": "larger_resident_batch_256_steps_64",
      "nstates": 256,
      "steps": 64,
      "resident_steps": true,
      "required": true
    },
    {
      "name": "larger_resident_batch_512_steps_64",
      "nstates": 512,
      "steps": 64,
      "resident_steps": true,
      "required": true
    }
  ],
  "correctness_policy": {
    "baseline": "one-state normalized CPU/GPU final-state equivalence has already passed for VeeR-EL2",
    "batched_gpu_runs": [
      "run exits with status 0",
      "dumped state byte size equals storage_size * nstates when dump is requested",
      "resident mode reports resident_mode=true",
      "speedup claim requires the matching CPU exact-loop larger resident baseline"
    ]
  },
  "performance_policy": {
    "record": [
      "nstates",
      "steps",
      "elapsed_ms",
      "state_steps_per_second"
    ],
    "non_claims": [
      "not proof of broad VeeR family support",
      "not a full RTL application throughput claim",
      "not a resident patch/script semantics claim"
    ]
  },
  "artifacts": {
    "report": "reports/veer_el2_larger_resident_workload_scaling.json",
    "generated_outputs_only": true
  },
  "next_action_on_pass": "run_veer_el2_cpu_exact_loop_larger_resident_workload_baseline",
  "next_action_on_fail": "diagnose_veer_el2_larger_resident_workload_scaling_failure"
}
