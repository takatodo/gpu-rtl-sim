# Status Memory

This file is intentionally a pointer, not a mirrored status document.

Current project status lives in:

- `docs/status.md`
- `config/selection.json`
- `README.md`

Do not copy current status text here. If an agent needs the current state, read
the canonical files above. Historical status details should be recovered from
git history or the tracked gate/report records instead of being duplicated in
this Codex-facing memory file.
